# E2E evidence — 2026-08-29

This run was executed from commit `7972ec9ab2c870e09633f0250674b1644641fd05` in the clean checkout.

Command:

```text
PLAYWRIGHT_HTML_OPEN=never bun run test:e2e
```

Result: **20 passed, 0 failed** in 23.7 seconds, using one worker.

Latest working-tree rerun of the same command executed 39 tests: **33 passed,
6 skipped, 0 failed** in 34.8 seconds. The six skips are the disposable
PostgreSQL/MySQL real-engine scenarios; their successful runs are recorded in
the database evidence document.

The latest full browser rerun executed **54 tests: 40 passed, 14 skipped, 0
failed** in 40.2 seconds. The skips remain environment-dependent real-engine
scenarios and are not treated as passed acceptance.

The latest focused real-browser reruns also passed: object explorer/search **1
pass in 8.3 seconds**, real security **4 passes in 10.3 seconds**, and SQL
export/import **2 passes**. These are local disposable-engine observations and
do not replace the remaining manual, performance, visual, or hosted-CI proof.

The latest mock browser batch for shell navigation, audit admin, query history,
database/schema/view management, and import/export completed **10 passes, 0
failures** in 12.3 seconds.

The latest configured real-engine batch completed **14/14 passes, 0 failures**
with disposable PostgreSQL/MySQL fixtures and the PostgreSQL native-tool
wrappers. It covered SQL export/import, real query/editor workflows, upload and
native backup/restore, and principal/privilege security flows.

The focused native backup-to-restore rerun completed **4/4 passes, 0 failures**.
For both MySQL and PostgreSQL it asserted a completed backup job, a valid
compressed artifact, restore roundtrip data, and the recorded `backup.completed`
audit event containing the target database name without credential material.

- `VIS-0015-AC8`: viewport boundary proof lulus pada 1024px dan 1023px; screenshot `test-results/visual-0015-shell-1024.png`.

Rerun terarah real security pada 2026-08-30 lulus **2/2 dalam 8,5 detik**
untuk grant/revoke privilege table melalui UI pada PostgreSQL dan MySQL. Test
memverifikasi efek privilege nyata dan kini dipetakan ke `E2E-0046-AC7`; tidak
ada credential yang ditulis ke evidence.

Pada rerun terbaru 2026-08-30 dengan environment disposable dan wrapper native
PostgreSQL yang eksplisit, `zz-real-restore.spec.ts` kembali lulus **4/4 dalam
21,6 detik**. Full suite pertama dengan konfigurasi wrapper yang sama
menjalankan **53 test** dan menemukan satu assertion revoke privilege PostgreSQL
yang sempat flaky karena listing grant masih memuat row lama. Rerun terisolasi
untuk test tersebut lulus **1/1 dalam 8,2 detik**. Setelah test diperbaiki agar
menunggu response `POST /security/grants/apply` yang selesai, full real E2E
berikutnya lulus **54/54 dalam 3,7 menit**, termasuk backup/restore kedua engine
dan privilege grant/revoke kedua engine.
Command: `MYADMIN_REAL_DATABASE_E2E=1
MYADMIN_TOOLS_PG_DUMP_PATH=$PWD/tests/fixtures/postgres-pg-dump.sh
MYADMIN_TOOLS_PSQL_PATH=$PWD/tests/fixtures/postgres-psql.sh bun run test:e2e`.

Rerun current pada 2026-08-30 dengan command dan fixture yang sama kembali
lulus **54/54 dalam 3,6 menit**. Run ini mencakup query editor, explorer/search,
data browser, schema/database/table/view management, security principal dan
privilege effects, export/import, serta native backup/restore. Ini adalah
bukti lokal terbaru; hosted CI, formal visual/accessibility review, dan
acceptance eksternal tetap terpisah.

Focused rerun real-engine terbaru pada 2026-08-30 untuk
`zz-real-query-editor.spec.ts` lulus **4/4 dalam 2,4 menit**. Tambahan pada
`E2E-0040-AC3` memaksa `POST /schemas` pada disposable MySQL yang capability
`schemas`-nya false dan membuktikan response HTTP `501` dengan kode publik
`SCHEMA_UNSUPPORTED`; flow PostgreSQL create/rename/drop tetap lulus.

- `PERF-0051-AC2`: `MYADMIN_POSTGRES_INTEGRATION=1 bun test --isolate tests/performance/monitoring-status.test.ts` lulus **1 pass, 4 assertions**; 10 status calls concurrent selesai dalam **2,89 ms** (threshold `<3000 ms`).
- `PERF-0051-AC4`: `bunx playwright test tests/e2e/web/monitoring-status.spec.ts` lulus **1 pass**; setelah aksi Test now, 0 status requests tambahan selama **1.200 ms** dalam 3 interval observasi.
- `PERF-0051-AC7`: `bunx playwright test tests/e2e/web/monitoring-status.spec.ts` lulus **1 pass**; network observation yang sama membuktikan tidak ada polling status tambahan setelah update latency.
- `E2E-0053-AC3`: `bun run test:e2e -- tests/e2e/web/security-headers.spec.ts` lulus **1 pass**; live API response memverifikasi CSP, nosniff, referrer policy, frame deny, dan `Cache-Control: no-store`.

Focused real query rerun pada 2026-08-30 lulus **1/1 dalam 2,5 menit** setelah
PostgreSQL query sessions memakai reserved client yang sama seperti MySQL.
Skenario `E2E-0033-AC9` menjalankan `BEGIN`, `INSERT`, `ROLLBACK`, lalu query
verifikasi pada `tabSessionId` yang sama untuk kedua engine; row hasil rollback
terbukti tidak tersimpan. Skenario yang sama menjalankan tiga statement dengan
error di tengah dan memverifikasi state `done/error/skipped` serta posisi error.
Ini menutup bukti AC-9 lokal; review acceptance penuh tetap terpisah.

The direct application-boundary test sweep completed **72 passes, 0 failures,
250 assertions** across `apps/server/test` and `apps/web/test`, covering query,
history, database/schema/table/view operations, security, workspace, and UI
policy helpers.

The mock-only rerun used to isolate local browser behavior was:

```text
bun run test:e2e -- --grep-invert 'real PostgreSQL|real query|real database|real security'
```

The latest rerun executed **34 tests: 33 passed, 1 skipped, 0 failed** in 34.5
seconds.
The remaining skip is an explicitly skipped real-engine test whose title does
not match the exclusion filter; no failure was hidden by the command.

Earlier working-tree rerun with `MYADMIN_REAL_DATABASE_E2E=1` executed all 3
real-engine tests: **3 passed, 0 skipped, 0 failed** in 2.4 minutes. This run exercised
the PostgreSQL and MySQL real-engine workflows, including query execution,
metadata/search, data browser, and table designer operations.

Focused rerun of the real-engine suites after adding schema, view, and security
flows executed **4 passed, 0 skipped, 0 failed** in 2.2 minutes.
The first test in `zz-real-query-editor.spec.ts` covered real database properties,
create/drop, query execution, search, data-browser/table-designer operations,
and rename/truncate/drop table flows on both engines; the second covered real
view creation, DDL preview, and the exact-name destructive confirmation gate;
the third covered foreign-key, composite-unique, and index-drop operations on
both engines. The fourth test in `zz-real-security.spec.ts` covered reset-password
and exact-name principal drop through the real Security UI; principal creation
was seeded through a validated API request because Chromium autofill corrupts the
dynamic password form, so create-UI acceptance is not claimed.

The latest focused rerun of these real security browser tests passed **4/4 in
10.4 seconds**. `E2E-0045` covers PostgreSQL and MySQL principal create, real
attribute update, reset-password, exact-name drop, and response redaction;
the MySQL flow also proves login with the rotated password. `E2E-0046` covers
real PostgreSQL and MySQL table `SELECT` grant and revoke through the UI,
including effective-grant introspection after each mutation. Credentials and
passwords are not recorded.

Focused real-browser view coverage also passed **1 test in 6.6 seconds** for
PostgreSQL view creation, provider validation/DDL preview, drop preview,
exact-name confirmation, and the final destructive UI DELETE (`204`). The
confirmation section now has an explicit stacking layer so the CodeMirror
surface cannot intercept the normal pointer click. This proves the PostgreSQL
UI path, not the full two-engine CRUD acceptance or visual/audit sign-off.

A second focused real-browser test passed **1 test in 6.6 seconds** for the
same create, validation/preview, exact-name confirmation, and destructive
DELETE (`204`) flow against disposable MySQL. Together these tests provide
real browser coverage for both provider engines; full data-open, alter,
authorization, audit, and performance acceptance remains separate.

The first test also covers PostgreSQL schema create, rename, and exact-name drop
through the Schema Management UI. The second covers PostgreSQL view creation,
provider validation/DDL preview, drop preview, and exact-name confirmation gate.
The schema flow includes a regression fix for CSRF validation through the
Angular development proxy.

Focused real-browser security coverage also passed **1 test in 6.7 seconds**
for PostgreSQL table privilege application and revocation. The test created
disposable table and principal fixtures, selected the database/table and
`SELECT` privilege in the Security UI, verified and applied the `GRANT SELECT`
preview, verified the effective grant via the API, then unchecked `SELECT`,
verified and applied the destructive `REVOKE SELECT` flow, and confirmed the
grant disappeared. This is local proof for `E2E-0046-AC2` and `E2E-0046-AC3`;
principal actor login and SELECT-vs-INSERT enforcement are now proven by the
real provider integration suite on both engines, while full browser actor flow
and security review remain unproven.

The real export integration `tests/integration/export/real-export.test.ts`
also passed **3 tests with 42 assertions** across disposable PostgreSQL,
MySQL 8.0, and MySQL latest. It
queued CSV and SQL exports from a real table, waited for completed jobs,
verified row counts and downloaded artifacts, and checked CSV rows plus SQL
DDL/INSERT content. It then dropped the table, uploaded the exact SQL artifact
through `ImportService`, waited for a completed three-statement import, and
verified two restored rows on each engine. This strengthens `IT-0047-AC1`, `IT-0047-AC2`,
`IT-0047-AC5`, `IT-0047-AC8`, `IT-0048-AC2`, and `IT-0048-AC8`; it is not UI
E2E, scale-performance, or security-specific proof.

The real query-editor workflow also starts a long-running query on both
disposable engines, confirms the execution reaches `running`, cancels it,
confirms the terminal `cancelled` state, and calls Explain through the same
tab session. The focused rerun completed with Playwright status `passed` and
no failed tests (`test-results/.last-run.json`). This is API-backed browser
proof. The mock query-editor E2E now also clicks `Cancel query`, verifies the
cancel request returns 200, and verifies the visible `Cancelled` state; the
same test clicks `Explain` and verifies the plan panel. It then reloads with
`capabilities.explain=false` and verifies Explain is disabled with its reason
tooltip. The provider-error branch remains unclaimed.

The latest focused real workflow rerun passed **1 test in 2.2 minutes**. It
covered `E2E-0035-AC1`, `AC2`, `AC5`, `AC6`, `AC7`, and `AC8` through real
PostgreSQL/MySQL execution and Explain/cancel API calls, plus the database and
schema workflows recorded in the same scenario. The dedicated mock rerun
separately covers the visible Cancel/Explain buttons and the Explain capability
gate.

Passed test files:

- `initial-setup.spec.ts`
- `login-session.spec.ts`
- `monitoring-status.spec.ts`
- `settings-preferences.spec.ts` (2 tests)

Focused `0052` rerun lulus **2/2**: admin settings policy dan sinkronisasi
theme antar-context, serta pergantian light/dark/system tanpa navigasi.
Pasangan unit/integration settings juga lulus **6 test dan 40 assertions**;
contract ID khusus `CT-0052-*` dan security suite khusus di luar authorization
coverage yang ada belum tersedia.

- `smoke.spec.ts`
- `user-management.spec.ts`
- `workspace-persistence.spec.ts` (3 tests)
- `z-audit-admin.spec.ts`
- `z-connection-manager.spec.ts`
- `z-shell-navigation.spec.ts` (4 tests)
- `zz-connection-lifecycle.spec.ts`
- `zz-backup-restore.spec.ts`
- `zz-data-browser.spec.ts`
- `zz-database-management.spec.ts`
- `zz-import-export.spec.ts`
- `zz-query-editor.spec.ts`
- `zz-query-history.spec.ts`
- `zz-schema-management.spec.ts`
- `zz-security.spec.ts`
- `zz-view-editor.spec.ts`
- `zzz-object-explorer.spec.ts`
- `zzzz-table-designer.spec.ts` (5 tests)

This evidence covers only E2E IDs declared by the tests that ran. It does not prove
visual, performance, manual, external, or real PostgreSQL/MySQL acceptance criteria.

The complete mock feature rerun on the working tree executed **21 tests: 21
passed, 0 failed** in 21.3 seconds. It covered settings/theme, shell
navigation, audit admin, backup/restore safeguards, data browser, import,
query editor/result grid/cancel/explain, history, schema, security, view,
object explorer/search, and table-designer/destructive flows. This is local
mock E2E evidence and does not replace real-engine, visual baseline,
accessibility, performance, or hosted acceptance proof.

The latest real-engine browser evidence covers eight real tests in
`zz-real-query-editor.spec.ts` and `zz-real-security.spec.ts` with disposable
PostgreSQL and MySQL fixtures, and Playwright reported `passed` with no failed
tests. The run covers query execution/cancel/EXPLAIN, object search and data
browser, database/schema/table operations, view workflows, index/constraint
workflows, principal reset/drop, and PostgreSQL/MySQL privilege grant/revoke.
Principal actor authorization (SELECT versus INSERT) is also proven by the
real provider integration suite on both engines. This is local real-browser
evidence; full visual/accessibility review, performance baselines, security
sign-off, and hosted acceptance remain separate.

The authenticated export/import route roundtrip additionally passed **2 tests,
0 failed**, one each for disposable PostgreSQL and MySQL. Each flow created a
table, queued SQL export, downloaded the artifact, dropped the table, uploaded
the same artifact, completed the SQL import job, and verified both typed rows
after restore. This proves local route and job integration, not visual review
or large scale performance.

Pada 2026-08-30, seluruh `zz-real-restore.spec.ts` lulus **4 test dalam 19,5
detik**. Upload-restore dan native backup-to-restore PostgreSQL/MySQL
memverifikasi job terminal, artifact gzip, validation, dan row hasil restore.
PostgreSQL native tool dideteksi melalui wrapper Docker disposable; smoke
operasional penuh dan acceptance eksternal tetap terbuka.

Run gabungan real-engine pada 2026-08-30 lulus **14 test, 0 gagal dalam 3,0
menit**. Cakupannya query execution/cancel/EXPLAIN, database/schema/table/view
operations, index/constraint, security principal dan grant/revoke, export/import,
upload-restore, serta native backup-to-restore pada PostgreSQL dan MySQL.
PostgreSQL native tools memakai wrapper Docker disposable; hasil ini tetap local
real-engine evidence dan tidak menggantikan hosted, visual/manual, signing,
atau clean-VM acceptance.

ID yang dibuktikan oleh run gabungan real-engine tersebut:

- `E2E-0032-AC6`: object search nyata pada PostgreSQL dan MySQL.
- `E2E-0031-AC8`: Explorer metadata, lazy reveal, dan object search/reveal pada PostgreSQL dan MySQL.
- `E2E-0035-AC1`, `E2E-0035-AC2`, `E2E-0035-AC5`, `E2E-0035-AC6`, `E2E-0035-AC7`, `E2E-0035-AC8`: cancel dan EXPLAIN nyata pada kedua engine.
- `E2E-0037-AC1`, `E2E-0038-AC9`, `E2E-0039-AC1`, `E2E-0039-AC2`, `E2E-0039-AC5`, `E2E-0040-AC1`, `E2E-0040-AC3`, `E2E-0040-AC4`, `E2E-0040-AC6`, `E2E-0041-AC8`, `E2E-0042-AC3`, `E2E-0042-AC5`, `E2E-0042-AC8`, `E2E-0043-AC1`, `E2E-0043-AC2`, `E2E-0043-AC5`: real database, schema, table, index, constraint, dan data workflows. `E2E-0040-AC3` juga memaksa endpoint schema pada MySQL dan menerima `501 SCHEMA_UNSUPPORTED`.
- `E2E-0044-AC1`, `E2E-0044-AC2`, `E2E-0044-AC6`, `E2E-0044-AC7`, `E2E-0044-AC8`: real view preview, create, dan drop.
- `E2E-0045-AC1`, `E2E-0045-AC2`, `E2E-0045-AC4`, `E2E-0045-AC5`, `E2E-0046-AC2`, `E2E-0046-AC3`: real principal lifecycle dan grant/revoke kedua engine.
- `E2E-0047-AC8`, `E2E-0048-AC8`: real export/import SQL route roundtrip kedua engine.
- `E2E-0049-AC1`, `E2E-0049-AC8`, `E2E-0050-AC2`, `E2E-0050-AC4`, `E2E-0050-AC7`: real upload-restore dan native backup-to-restore kedua engine.

Index machine-readable per ID untuk matrix:

- `E2E-0031-AC8`: real query-editor suite 4/4 pass; Explorer workflow berjalan pada kedua engine.
- `E2E-0032-AC6`: run gabungan real-engine 14/14 pass.
- `E2E-0035-AC1`: run gabungan real-engine 14/14 pass.
- `E2E-0035-AC2`: run gabungan real-engine 14/14 pass.
- `E2E-0035-AC5`: run gabungan real-engine 14/14 pass.
- `E2E-0035-AC6`: run gabungan real-engine 14/14 pass.
- `E2E-0035-AC7`: run gabungan real-engine 14/14 pass.
- `E2E-0035-AC8`: run gabungan real-engine 14/14 pass.
- `E2E-0037-AC1`: run gabungan real-engine 14/14 pass.
- `E2E-0038-AC9`: run gabungan real-engine 14/14 pass.
- `E2E-0038-AC9`: `MYADMIN_REAL_DATABASE_E2E=1 bun run test:e2e -- tests/e2e/web/zz-real-data-browser-typed.spec.ts` lulus **1 pass dalam 9,5 detik**; browser mengedit JSON, mempertahankan NULL eksplisit, dan memverifikasi stale-row conflict pada PostgreSQL serta MySQL.
- `E2E-0039-AC1`: run gabungan real-engine 14/14 pass.
- `E2E-0039-AC2`: run gabungan real-engine 14/14 pass.
- `E2E-0039-AC5`: run gabungan real-engine 14/14 pass.
- `E2E-0040-AC1`: run gabungan real-engine 14/14 pass.
- `E2E-0040-AC3`: run gabungan real-engine 14/14 pass.
- `E2E-0040-AC4`: run gabungan real-engine 14/14 pass.
- `E2E-0040-AC6`: run gabungan real-engine 14/14 pass.
- `E2E-0041-AC8`: run gabungan real-engine 14/14 pass.
- `E2E-0042-AC3`: run gabungan real-engine 14/14 pass.
- `E2E-0042-AC4`: `bun run test:e2e -- tests/e2e/web/zzzz-table-designer.spec.ts` lulus 10/10; browser mengisi nama dan ekspresi check, mem-preview DDL, memeriksa change set `addConstraint`, serta memastikan tombol check disabled dengan alasan capability pada MySQL.
- `E2E-0042-AC5`: run gabungan real-engine 14/14 pass.
- `E2E-0042-AC8`: run gabungan real-engine 14/14 pass.
- `E2E-0043-AC1`: run gabungan real-engine 14/14 pass.
- `E2E-0043-AC2`: run gabungan real-engine 14/14 pass.
- `E2E-0043-AC5`: run gabungan real-engine 14/14 pass.
- `E2E-0044-AC1`: run gabungan real-engine 14/14 pass.
- `E2E-0044-AC2`: run gabungan real-engine 14/14 pass.
- `E2E-0044-AC6`: run gabungan real-engine 14/14 pass.
- `E2E-0044-AC7`: run gabungan real-engine 14/14 pass.
- `E2E-0044-AC8`: run gabungan real-engine 14/14 pass.
- `E2E-0045-AC1`: run gabungan real-engine 14/14 pass.
- `E2E-0045-AC2`: run gabungan real-engine 14/14 pass.
- `E2E-0045-AC4`: run gabungan real-engine 14/14 pass.
- `E2E-0045-AC5`: run gabungan real-engine 14/14 pass.
- `E2E-0046-AC2`: run gabungan real-engine 14/14 pass.
- `E2E-0046-AC3`: run gabungan real-engine 14/14 pass.
- `E2E-0047-AC8`: run gabungan real-engine 14/14 pass.
- `E2E-0048-AC8`: run gabungan real-engine 14/14 pass.
- `E2E-0049-AC1`: run gabungan real-engine 14/14 pass.
- `E2E-0049-AC8`: run gabungan real-engine 14/14 pass.
- `E2E-0050-AC2`: run gabungan real-engine 14/14 pass.
- `E2E-0050-AC4`: run gabungan real-engine 14/14 pass.
- `E2E-0050-AC7`: run gabungan real-engine 14/14 pass.
- `PERF-0034-AC1`: ResultGrid 5.000-row local performance pass.
- `PERF-0034-AC8`: ResultGrid 5.000-row local performance pass.

* **E2E-0033-AC3, E2E-0033-AC9**: `PLAYWRIGHT_HTML_OPEN=never bunx playwright test tests/e2e/web/zz-query-editor.spec.ts` lulus **1 pass dalam 6,8 detik**. Fixture mengembalikan metadata objects, request `query/metadata?kind=objects` teramati, popup CodeMirror menampilkan `orders`, dan test melanjutkan query multi statement, cancel, EXPLAIN, typed result, clipboard, serta export. Ini membuktikan jalur mock browser, bukan keseluruhan skenario AC-9 lintas engine.
* **Real query regression rerun**: dengan `MYADMIN_REAL_DATABASE_E2E=1` dan tool PostgreSQL disposable, `tests/e2e/web/zz-real-query-editor.spec.ts --grep "execute real database workflows"` lulus **1 pass dalam 2,8 menit** setelah fixture metadata 4.000 tabel dibangun dan dibersihkan. Cakupan real query, database, schema, table, view, index, cancel, EXPLAIN, data browser, dan destructive workflow tetap hijau; autocomplete popup real tidak diklaim dari run ini.

* **E2E-0039-AC3, E2E-0039-AC6**: skenario real database workflow yang sama membuat dan menghapus database melalui UI pada PostgreSQL dan MySQL, mengamati status HTTP create/drop, serta mengetik ulang nama database pada dialog destructive; ID coverage sekarang dicatat eksplisit pada source test.

* **E2E-0048-AC5**: `bun run test:e2e -- tests/e2e/web/zz-import-export.spec.ts` lulus **1 pass dalam 7,9 detik**; browser membatalkan import job queued, mengamati request cancel, memuat ulang daftar job, dan memastikan job tidak lagi ditampilkan. Ini adalah proof mock-browser; rollback/partial-result nyata tetap dibuktikan oleh integration test.

* **E2E-0047-AC4**: test browser import/export yang sama lulus **1 pass** dan membatalkan export job yang tampil pada panel jobs, memverifikasi request cancel serta state cancelled pada UI; cleanup artifact parsial tetap dibuktikan oleh unit/integration export.

* **SEC-0048-AC8**: `MYADMIN_REAL_DATABASE_E2E=1 bun run test:e2e -- tests/e2e/web/zz-real-import-export.spec.ts` lulus **2/2** (PostgreSQL dan MySQL); SQL artifact dan completed import job response diverifikasi tidak memuat credential target.

* **E2E-0049-AC5**: `bun run test:e2e -- tests/e2e/web/zz-backup-restore.spec.ts` lulus **1 pass dalam 8,2 detik**; browser mengunduh artifact backup dengan nama file yang benar dan menghapusnya melalui prompt konfirmasi nama yang tepat, lalu artifact hilang dari daftar. Ini melengkapi proof UI; ownership/security tetap mengikuti test route terpisah.

* **Real-browser rerun 2026-08-30**: `MYADMIN_REAL_DATABASE_E2E=1 bun run test:e2e -- tests/e2e/web/zz-real-query-editor.spec.ts` lulus **4 pass dalam 2,5 menit**. Workflow mencakup real PostgreSQL dan MySQL untuk query execution, data browser, database/schema/table management, view CRUD, table designer index/constraint, cancel, EXPLAIN, dan destructive confirmation. Test ini tetap bukan bukti hosted CI atau manual visual/accessibility sign-off.

* **Native restore rerun 2026-08-30**: `MYADMIN_REAL_DATABASE_E2E=1 MYADMIN_TOOLS_PG_DUMP_PATH=/Users/ojiepermana/Development/ojiepermana/myadmin/tests/fixtures/postgres-pg-dump.sh MYADMIN_TOOLS_PSQL_PATH=/Users/ojiepermana/Development/ojiepermana/myadmin/tests/fixtures/postgres-psql.sh bun run test:e2e -- tests/e2e/web/zz-real-restore.spec.ts --grep 'native'` lulus **2 pass dalam 18,7 detik**; backup-to-restore native PostgreSQL dan MySQL selesai sampai verifikasi baris. Ini memperkuat Spec 0049/0050 lokal, tanpa mengubah kebutuhan smoke clean-platform.

* **PERF-0031-AC7**: `PLAYWRIGHT_HTML_OPEN=never bun run test:e2e -- tests/e2e/web/zzz-object-explorer.spec.ts` lulus **1 pass dalam 10,3 detik**. Test mengukur hasil search pertama pada dataset 2.000 object per engine dan mensyaratkan respons <3 detik untuk PostgreSQL dan MySQL; ini adalah smoke performance lokal, bukan benchmark lintas mesin formal.

- `PERF-0031-AC7`: E2E explorer mengukur dataset 2.000 object per engine dan lulus dengan ambang hasil search pertama <3 detik pada PostgreSQL dan MySQL; smoke performance lokal, bukan benchmark lintas mesin formal.
- `VIS-0031-AC7`: `test-results/visual-0031-explorer.png` pada viewport Playwright 1280x720 diinspeksi setelah lazy tree, pagination search, dan action affordance terlihat; visual lokal lulus, tetapi bukan sign-off accessibility atau cross-browser.
- `E2E-0031-AC2`: focused rerun `PLAYWRIGHT_HTML_OPEN=never bun run test:e2e -- tests/e2e/web/zzz-object-explorer.spec.ts` lulus **1 pass dalam 8,5 detik**; provider-driven tree, capability-gated schema, lazy expansion, dan paginated search kembali terverifikasi. Manual review engine-agnostic tetap terpisah.
- `E2E-0042-AC7`: `bun run test:e2e -- tests/e2e/web/zzzz-table-designer.spec.ts` lulus **11 pass dalam 12,6 detik**; browser menerapkan PK melalui table designer, mengamati metadata refresh, lalu memuat Data Browser dengan `rowIdentity` primary dan tombol Add row aktif.
- `E2E-0015-AC5`: `bun run test:e2e -- tests/e2e/web/z-shell-navigation.spec.ts` lulus **6 pass dalam 12,8 detik**; browser memuat seluruh 18 protected V1 route dan memverifikasi landmark `<main>` pada setiap route.
- `E2E-0015-AC4`: `PLAYWRIGHT_HTML_OPEN=never bun run test:e2e -- tests/e2e/web/z-shell-navigation.spec.ts` lulus **6 pass dalam 12,0 detik**; context menu tunggal, keyboard open, dan Escape close lulus. Screenshot visual tersimpan sebagai `test-results/visual-0015-context-menu.png`.
- `E2E-0015-AC7`: suite yang sama lulus **6 pass dalam 12,0 detik**; keyboard navigation dan shortcuts dialog lulus. Screenshot visual tersimpan sebagai `test-results/visual-0015-keyboard-dialog.png`.
- `E2E-0045-AC1`, `E2E-0045-AC2`, `E2E-0045-AC4`, `E2E-0045-AC5`, `E2E-0045-AC8`, `E2E-0046-AC2`, `E2E-0046-AC3`, dan `SEC-0045-AC8`: `MYADMIN_REAL_DATABASE_E2E=1 PLAYWRIGHT_HTML_OPEN=never bun run test:e2e -- tests/e2e/web/zz-real-security.spec.ts` lulus **4 pass dalam 12,6 detik** pada PostgreSQL 55433 dan MySQL 3380; principal lifecycle, reset/drop, response redaction, serta grant/revoke UI terverifikasi dengan fixture disposable.

- **Mock E2E sweep 2026-08-30**: `bunx playwright test --config playwright.config.ts --grep-invert 'real database|real PostgreSQL|real MySQL|performance|NFR'` lulus **44 pass, 5 skip, 0 fail dalam 44,5 detik**. Cakupan mencakup setup/login, settings, shell navigation, audit, backup/restore safeguards, data browser, query editor/history, schema/database management, security, view editor, object explorer, dan table designer. Lima skip adalah suite real-engine/performance yang sengaja dikeluarkan dari command ini.
- **E2E-0036 regression fix**: `bunx playwright test --config playwright.config.ts tests/e2e/web/zz-query-history.spec.ts` lulus **1 pass, 0 fail dalam 7,9 detik** setelah assertion memilih teks toast sukses secara exact, bukan dua elemen `role=status` yang juga mencakup loading skeleton.
- **Dev/visual/performance E2E 2026-08-30**: `MYADMIN_E2E_WEB_CONFIGURATION=development bunx playwright test --config playwright.config.ts tests/e2e/web/ui-foundation-demo.spec.ts tests/e2e/web/zz-result-grid-performance.spec.ts tests/e2e/web/zzz-object-explorer.spec.ts tests/e2e/web/monitoring-status.spec.ts` lulus **4 pass, 0 fail dalam 33,1 detik**. Foundation demo light/dark, ResultGrid 5.000 typed rows (247,3 ms), object explorer search 2.000 objects per engine, dan monitoring status/performance terverifikasi; ini tetap evidence lokal.
- **Full browser E2E 2026-08-30**: `MYADMIN_REAL_DATABASE_E2E=1 bunx playwright test --config playwright.config.ts` menghasilkan **60 pass, 2 skip, 1 fail dalam 4,4 menit**. Satu failure PostgreSQL native backup-to-restore berasal dari capability tool yang tidak aktif karena wrapper PostgreSQL tidak diberikan pada command ini; bukan hasil yang boleh diklaim hijau penuh.
- **Native restore rerun dengan environment tool lengkap**: `MYADMIN_REAL_DATABASE_E2E=1 MYADMIN_TOOLS_PG_DUMP_PATH=$PWD/tests/fixtures/postgres-pg-dump.sh MYADMIN_TOOLS_PSQL_PATH=$PWD/tests/fixtures/postgres-psql.sh bunx playwright test --config playwright.config.ts tests/e2e/web/zz-real-restore.spec.ts --grep 'native'` lulus **2 pass, 0 fail dalam 20,6 detik** untuk MySQL dan PostgreSQL. Ini memulihkan bukti native lokal, tetapi tidak menutup clean-platform atau hosted-release acceptance.

- **Real export/import dan restore rerun 2026-08-30**: dengan `MYADMIN_REAL_DATABASE_E2E=1`, PostgreSQL native wrapper (`pg_dump`/`psql`), dan fixture MySQL disposable, `bun run test:e2e -- tests/e2e/web/zz-real-import-export.spec.ts tests/e2e/web/zz-real-restore.spec.ts` lulus **6 pass, 0 fail dalam 24,4 detik**. Dua engine menyelesaikan SQL export/import route roundtrip, upload restore, native gzip backup, native restore, dan row verification; ini tetap evidence lokal, bukan hosted release atau clean-platform proof.
