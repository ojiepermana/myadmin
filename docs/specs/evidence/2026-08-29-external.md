# External and release proof — 2026-08-29

## CI

- `SMOKE-0003-AC2`: hosted Contract workflow on commit `2544dcd037f9996c289afbc431f28682192d1276` completed successfully; its contract test job ran validation and reported 76 passing tests. [Contract run 33288273267](https://github.com/ojiepermana/myadmin/actions/runs/33288273267)
- `SMOKE-0004-AC2`: hosted Contract workflow on commit `2544dcd037f9996c289afbc431f28682192d1276` completed successfully with the generated-type drift check. [Contract run 33288273267](https://github.com/ojiepermana/myadmin/actions/runs/33288273267)
- `SMOKE-0004-AC7`: hosted `contract.yml` workflow on commit `2544dcd037f9996c289afbc431f28682192d1276` completed successfully. [Contract run 33288273267](https://github.com/ojiepermana/myadmin/actions/runs/33288273267)
- `SMOKE-0053-AC2`: hosted Security workflow on commit `2544dcd037f9996c289afbc431f28682192d1276` completed successfully with the repository secret scan. [Security run 33288273229](https://github.com/ojiepermana/myadmin/actions/runs/33288273229)

- Remote CI snapshot before this audit fix on commit `2299b6822718ea168d769d7c4e9c43825ae6860f`: **cancelled after the unit-test job exceeded the timeout** — [CI run](https://github.com/ojiepermana/myadmin/actions/runs/33219851024).
- Remote Security on the same commit: **passed** — [Security run](https://github.com/ojiepermana/myadmin/actions/runs/33219851113).
- The latest complete CI, Contract, and Security baseline before the fix was commit `7972ec9ab2c870e09633f0250674b1644641fd05`: **passed** — [CI](https://github.com/ojiepermana/myadmin/actions/runs/33218476556), [Contract](https://github.com/ojiepermana/myadmin/actions/runs/33218476342), [Security](https://github.com/ojiepermana/myadmin/actions/runs/33218476247).
- Previous CI on commit `b82a6c0706f70cbffab6424f92471bf9d27a8907`: **passed** — [CI run](https://github.com/ojiepermana/myadmin/actions/runs/33217005672).
- Previous Contract on commit `b82a6c0706f70cbffab6424f92471bf9d27a8907`: **passed** — [Contract run](https://github.com/ojiepermana/myadmin/actions/runs/33217005665).
- Previous Security on commit `b82a6c0706f70cbffab6424f92471bf9d27a8907`: **passed** — [Security run](https://github.com/ojiepermana/myadmin/actions/runs/33217005649).
- Previous CI on commit `006c559b11f899b3710b058f1e15cecb344554fc`: **passed** — [CI run](https://github.com/ojiepermana/myadmin/actions/runs/33215355362).
- Previous Security on commit `006c559b11f899b3710b058f1e15cecb344554fc`: **passed** — [Security run](https://github.com/ojiepermana/myadmin/actions/runs/33215355339).
- CI on commit `1938238e9cd089073dca4240282338fe5452e669`: **passed**.
- CI on commit `5cf5bb8d4fa761dce1acc897454a3f1a6f19aced`: **passed**.
- Security on the same commit: **passed**.
- Security on commit `5cf5bb8d4fa761dce1acc897454a3f1a6f19aced`: **passed**.
- Contract on the preceding application commit `5c128d6a7ce3d9432a910e52bb7c2ad41885dc64`: **passed**; the later commits only changed audit evidence and its generator.

Runs: [CI](https://github.com/ojiepermana/myadmin/actions/runs/33213399739),
[Security](https://github.com/ojiepermana/myadmin/actions/runs/33213399731),
[Contract](https://github.com/ojiepermana/myadmin/actions/runs/33212297220).

The codegen and contract-bundle race fix in this audit is not considered
externally verified until the pushed commit receives a fresh successful CI run.

Current working-tree root regression after the security UI capability change:
**664 pass, 0 fail, 4.532 assertions across 143 files in 91.08 seconds** with
disposable PostgreSQL and MySQL fixtures enabled. This is local evidence only.

## Final local gates

- `SMOKE-0014-AC1`: `bun test scripts/quality/ui-foundation-smoke.test.ts` passed the pinned foundation-package and theme-entrypoint invariant locally; this does not replace visual/manual theme proof.
- `SMOKE-0014-AC6`: `bun test --isolate scripts/quality/ui-foundation-smoke.test.ts` passed the dev-route/template smoke invariant; the production route factory excludes the dev demo while the demo template retains the required foundation primitives. Development-browser rendering is separately covered by `E2E-0014-AC6`/`VIS-0014-AC6`.
- `MANUAL-0009-AC6`: Codex reviewed `AuditRepository` in `packages/internal-domain/src/ports/repositories/index.ts`, the SQLite append-only triggers in `packages/internal-sqlite/src/migrations/0001-initial.ts`, and application source references. The local review found append/query-only repository behavior and no application audit update/delete path; it is not external sign-off.
- `MANUAL-0019-AC5`: Codex reviewed the same append/query-only audit boundary, SQLite append-only triggers, and audit package usage. The local review found no direct audit mutation path in application code; it is not external sign-off.
- `SMOKE-0014-AC5`: `bun test --isolate scripts/quality/ui-foundation-smoke.test.ts` passed the V1 capability-audit completeness invariant with 32 assertions; all 16 listed needs have an API or explicit gap, and the tree gap has a foundation-package follow-up that forbids a generic local fallback.
- `MANUAL-0014-AC5`: Codex reviewed `docs/architecture/ui-foundation-capability-audit.md` against the installed `@ojiepermana/angular@22.1.7` entrypoint declarations and the documented boundary rule. The review is local and reproducible; it is not an external package-owner sign-off.
- `MANUAL-0014-AC4`: Codex reviewed `apps/web/src/app/core/theme/theme.config.ts`, `myadmin-theme.ts`, and the development demo against the foundation extension API. Accent, neutral palette, radius, spacing, typography, and mode are configured through `provideUiTheme`, with no direct design-system override import; this is a local review, not package-owner sign-off.
- `MANUAL-0015-AC1`: Codex reviewed the shell implementation and regenerated `test-results/visual-0015-shell-1280.png`; top bar/branding, user placeholder, theme toggle, sidebar, workspace tab, activity panel, and status bar are visible without clipping at 1280px. This is a local review, not formal accessibility or cross-browser sign-off.
- `CT-0021-AC8`: `bun test tests/contract/database-provider-acceptance.test.ts` passed the port-contract completeness assertion; all 15 port rows and the cross-port invariants are present in `docs/specs/0021-database-core-contracts/port-contracts.md`.
- `MANUAL-0021-AC8`: Codex reviewed `port-contracts.md` against the `database-core` contract files and confirmed each port states required behavior plus unsupported/failure handling. This is a local review, not an external reviewer sign-off.
- `VIS-0014-AC2`: `test-results/visual-0014-light.png` and `test-results/visual-0014-dark.png` were regenerated by the focused settings E2E and inspected at the locked state; both theme renderings are intact, without claiming formal reviewer sign-off.
- `VIS-0015-AC1`: `test-results/visual-0015-shell-1280.png` was regenerated and inspected at 1280px, showing the top bar, sidebar, host tab area, activity panel, and status bar without visible clipping; accessibility/manual sign-off remains open.
- `VIS-0015-AC2`: `test-results/visual-0015-shell-1280.png` records the shell after resize/fold interaction in the focused E2E flow; this is local visual evidence, not a cross-browser baseline.
- `VIS-0015-AC4`: `test-results/visual-0015-context-menu.png` was regenerated and inspected after opening the workspace context menu; the single visible menu contains the expected actions without visible clipping. This is local visual evidence, not formal accessibility or cross-browser sign-off.
- `VIS-0015-AC7`: `test-results/visual-0015-keyboard-dialog.png` was regenerated and inspected after keyboard navigation opened the shortcuts dialog; the dialog, keyboard affordances, focus ring, and close controls are visible. This is local visual evidence, not formal accessibility or cross-browser sign-off.
- `VIS-0031-AC4`: `test-results/visual-0031-explorer.png` was regenerated by the focused explorer E2E at 1280x720 and inspected; the virtualized tree, type icons, table list, and activity panel render without visible clipping. This is local visual evidence, not formal accessibility or cross-browser sign-off.
- `VIS-0033-AC2`: `test-results/visual-0033-query-editor.png` was regenerated by the focused query-editor E2E after the SQL editor and PostgreSQL connection state were visible; CodeMirror editor chrome and the stable editor surface were captured at the locked Playwright viewport. This is local visual evidence, not formal accessibility or cross-browser sign-off.
- `VIS-0034-AC1`: `test-results/visual-0034-result-grid.png` was regenerated by the focused 5,000-row ResultGrid E2E after the virtual grid, typed cells, and bounded rendering state were visible at the locked Playwright viewport. This is local visual evidence, not formal accessibility or cross-browser sign-off.
- `VIS-0034-AC7`: `test-results/visual-0034-accessibility.png` was regenerated by the focused query-editor E2E after keyboard navigation moved focus to the typed `NULL` cell in the ResultGrid; the same flow asserts the grid row/column ARIA counts and focused cell. This is local visual evidence, not formal contrast or screen-reader sign-off.
- `VIS-0036-AC7`: `test-results/visual-0036-query-history.png` was regenerated by the focused query-history E2E after the history result, completion status, and retention limit were visible. This is local visual evidence, not formal accessibility or cross-browser sign-off.
- `VIS-0037-AC6`: `test-results/visual-0037-data-browser.png` was regenerated by the focused data-browser E2E after bounded pagination reached Page 2; the same flow asserts the page request offset and filter/sort state. This is local visual evidence, not formal accessibility or cross-browser sign-off.
- `VIS-0051-AC1`: `test-results/visual-0051-monitoring-card.png` was regenerated from the focused monitoring E2E and inspected; the card shows provider/connection identity, Connected state, server version, database, uptime, latency, last error, and Test now control without clipping. This is local visual evidence, not formal accessibility or cross-browser sign-off.
- `IT-0049-AC6`: `bun test tests/integration/backup/backup-service.test.ts` passed the real service boundary with `JobManager`, artifact store, executor, and audit repository; it asserted `backup.started` then `backup.completed` and confirmed the credential was absent from audit data.
- `VIS-0014-AC6`: development build with `environment.devDemo=true` rendered `/__dev/ui-foundation` at 1280x900; `visual-0014-dev-demo-light.png` and `visual-0014-dev-demo-dark.png` were inspected after selecting light/system and dark modes, with foundation status, theme controls, composition primitives, and capability pulse present.
- `VIS-0014-AC6`: fresh development-configuration browser run lulus **1 pass dalam 6,6 detik**; light/dark demo screenshots were inspected at 1280x900 and showed the internal foundation route, theme control, foundation status, and shell composition. This remains local visual evidence, not formal accessibility or clean-platform smoke sign-off.
- `VIS-0014-AC4`: the same fresh development demo screenshots show the MyAdmin identity, accent color, radius/spacing treatment, typography, and foundation status rendered through the configured extension API; this is local visual evidence, while the required manual audit remains open.
- `E2E-0037-AC6`: focused data-browser browser flow passed **1/1**; the mock read response exposed `hasMore`, keyboard activation moved the UI to Page 2, and the captured `/api/v1/data/read` request used bounded paging (`limit: 100`, `offset: 100`). This is local mock UI evidence and does not prove view read-only or two-engine acceptance.
- `E2E-0037-AC7`: the same focused flow reopened a view reference and asserted the read-only label plus absence of the Add row action. This is local mock UI evidence; real view permissions and two-engine acceptance remain open.

### Latest working-tree rerun

The historical counts below are preserved for their original commit. A rerun
An earlier working-tree run with all disposable database fixtures enabled produced **645 pass, 0 skip, 0 fail**. The latest rerun, after preparing the disposable MySQL fixture privileges, produced **649 pass, 0 skip, 0 fail** according to the JUnit aggregate. The latest contract command produced **71 pass, 0 fail** contract tests, and
the subsequent restore-capability change was followed by a fresh root run of **658 pass, 0 fail, 4,507 assertions across 142 files**. This historical result remains local evidence only; hosted CI and external acceptance are not established.
**40 pass, 0 fail** security tests. The environment-dependent skips remain explicitly recorded rather than
being treated as acceptance proof.

- The earlier baseline `bun run test`: **554 pass, 8 skip, 0 fail** across 562 tests; production web build passed with an 893.70 kB initial bundle.
- `bun run test:contract`: **71 pass, 0 fail** on the latest working-tree rerun.
- `bun test tests/security`: **40 pass, 0 fail**.
- `PLAYWRIGHT_HTML_OPEN=never bun run test:e2e`: **20 pass, 0 fail** in 23.7 seconds; detailed evidence is in `2026-08-29-e2e.md`.
- `bun run lint`, `bun run format:check`, `bun run typecheck`, `bun run check:boundaries`, `bun run check:manifests`, `bun run check:contract-drift`, and `bun run validate-contract`: passed.
- Additional current-tree checks: `release.yml` parsed successfully as YAML;
  `bun run check:contract-drift` and `bun run validate-contract` completed with
  exit code 0. `actionlint` is not installed on this host, so action-specific
  hosted workflow validation remains unverified.

## Binary targets

`bun run build:binaries` completed for all five targets: Linux x64, Linux arm64,
macOS x64, macOS arm64, and Windows x64. `bun run checksums` and
`bun run release:sizes` also completed. Local macOS arm64 binary smoke passed
health, embedded SPA, setup, login, auth, shutdown, and doctor checks.

`bun run build:web:release` and `bun run embed:web` completed, embedding 56 web
assets. A second complete `bun run build:binaries` plus `bun run checksums` was
byte for byte identical to the first checksum file, proving deterministic
checksums for the repeated local build.

The database portion of binary smoke was not claimed because no disposable
database URL was available in the first run. A later database-backed run is
recorded below and in `2026-08-29-database.md`.

### Local Docker AC-4 proof

Docker image builds completed locally for both `runtime` and `tools` targets.
The runtime image was built and smoke-tested for both `linux/amd64` and
`linux/arm64`; the AMD64 image was built explicitly with `--platform
linux/amd64` because this host is ARM64. The initial mismatched-platform smoke
failed with the expected ELF loader error and was not counted. Both corrected
containers returned `{"status":"ok","version":"0.1.0"}` from `/health`, ran
as UID/GID `65532:65532`, and exposed the `/data` volume. The tools image also
built on the native ARM64 host. A separate `docker buildx build --platform
linux/amd64,linux/arm64 --target runtime --output type=oci` produced a local
OCI index containing both Linux platforms. These results prove local image
composition and runtime smoke only. A corresponding buildx OCI export for the
`tools` target also contained both Linux platforms and completed the native
client installation for each architecture. A native ARM64 `tools` container
also exposed `/usr/bin/pg_dump`, `/usr/bin/pg_restore`, `/usr/bin/psql`,
`/usr/bin/mysqldump`, and `/usr/bin/mysql` while running as UID/GID
`65532:65532`. GHCR publication and hosted release manifest verification remain
unverified. The explicitly platformed AMD64 tools container independently
exposed the same five client paths while running as UID/GID `65532:65532`.

### Local service-template proof

`plutil -lint distribution/service/com.myadmin.server.plist` passed. The current
macOS host does not provide `systemd-analyze`, so systemd installation and
runtime verification remain unproven; template invariants are covered by the
distribution release tests.

Database-backed binary smoke then passed for the macOS arm64 binary against
PostgreSQL 17.7, PostgreSQL 18.1, MySQL 8.0.43, and MySQL 8.4.6. Each run used
`--require-database` and exercised health, embedded SPA, setup, login, auth,
database connection, shutdown, and doctor.

Latest working-tree rerun of `scripts/verify/smoke-binary.ts` independently
passed the same database-backed flow four times against PostgreSQL ports
55433/55432 and MySQL ports 3380/3384 using `dist/binaries/macos-arm64/myadmin`.
The four runs exited 0 with no optional database path, so this local proof is
current; hosted release workflow and clean-platform acceptance remain open.

Evidence IDs demonstrated by this report:

- `SMOKE-0001-AC1`: current CI passed `bun install --frozen-lockfile` from a clean checkout using the single root manifest.
- `SMOKE-0001-AC2`: current CI passed the root `bun run typecheck` gate across the source modules.
- `SMOKE-0001-AC3`: current CI passed the root `bun run test` gate, whose first step is the production `bun run build:web`.
- `SMOKE-0001-AC9`: current CI passed the root manifest, build, and test gates that exercise the shared source resolution.
- `SMOKE-0002-AC1`: current clean CI quality job passed install, lint, format, typecheck, boundaries, manifests, and unit tests.
- `SMOKE-0002-AC7`: current CI passed on push at commit `7972ec9`.
- `SMOKE-0002-AC9`: current clean CI quality job passed the manifest check.
- `SMOKE-0053-AC8`: hosted Security workflow passed its scan, authorization, contract, drift, and security test gates on commit `2544dcd037f9996c289afbc431f28682192d1276`. [Security run 33288273229](https://github.com/ojiepermana/myadmin/actions/runs/33288273229)
- `SMOKE-0053-AC8`: `bun test --isolate tests/quality/distribution-release.test.ts` passed the local security-workflow wiring invariant; the workflow includes frozen install, secret scan, authorization-matrix check, contract validation/drift, and the security suite. Hosted execution remains unclaimed.
- `SMOKE-0054-AC2`: all five binary targets compiled successfully.
- `SMOKE-0054-AC1`: release web build and asset embedding completed, and the resulting SPA was served by binary smoke.
- `SMOKE-0054-AC3`: two consecutive five target builds produced identical checksum files.
- `SMOKE-0054-AC4`: database-backed binary smoke passed on both PostgreSQL and both MySQL fixture generations.
- `SMOKE-0054-AC4`: fresh rerun on 2026-08-30 passed twice with `--require-database` using `dist/binaries/macos-arm64/myadmin`: PostgreSQL disposable port 55433 and MySQL 8.0 disposable port 3380; each verified health, embedded SPA, setup/login/auth, database connect, SIGTERM shutdown, and doctor.
- `SMOKE-0054-AC6`: embedded SPA smoke and per-target release size report passed locally.

## Docker

- `docker buildx build --check --file distribution/docker/Dockerfile .`: passed.
- Local runtime image build passed with the Linux x64 artifact using
  `docker buildx build --platform linux/amd64 --file distribution/docker/Dockerfile --target runtime --tag myadmin:local-smoke --load .`.
- A clean container using a `/data` tmpfs passed `/health` (`{"status":"ok","version":"0.1.0"}`), `/api/v1/setup/status`, and ran as UID/GID `65532:65532` with `/data` mode `750`. The host is macOS ARM64, so this is an amd64 runtime smoke through Docker emulation; multi-architecture publication and clean-host acceptance remain unproven.
- The local `tools` target also built successfully and, in a clean amd64 container, exposed PostgreSQL client `15.19` and MariaDB/MySQL client `10.11`; the container remained non-root with the same `/data` permissions.
- Multi-architecture OCI exports for both `runtime` and `tools` completed with `docker buildx build --platform linux/amd64,linux/arm64 --output type=oci`; each `index.json` contained `linux/amd64` and `linux/arm64` manifests. This proves local composition, not registry publication.

- `SMOKE-0055-AC4`: local Docker runtime/tools builds, clean-container smoke, and amd64+arm64 OCI composition passed; registry publication remains unproven.
- `SMOKE-0055-AC8`: a clean local runtime container completed setup admin, login, authenticated `/auth/me`, PostgreSQL connection creation, and connection-open (`status: connected`) against the disposable fixture; this is local-built-image evidence, not release-artifact or clean-platform acceptance.
- A native `linux/arm64` runtime image was also loaded and smoke-tested on the macOS ARM64 host: health, setup admin, login, authenticated `/auth/me`, UID/GID `65532:65532`, and `/data` mode `750` passed without amd64 emulation.

- `MANUAL-0031-AC2`: Codex reviewed the object-explorer feature source and confirmed the UI has no PostgreSQL/MySQL engine-name branching; provider data and `capabilities.schemas` drive the tree. Combined with the inspected explorer screenshot and real two-engine run, this is local review evidence, not external sign-off.

## Signing and service runners

## Hosted CI follow-up

Hosted `CI` workflow run `33288273216` on commit `2544dcd037f9996c289afbc431f28682192d1276` completed with failure in the `quality` job because the realtime WebSocket integration case `IT-0029-AC4`, `IT-0029-AC7`, and `SEC-0029-AC7` exceeded its 10-second test timeout. The same focused file passed locally with 5 tests and 0 failures on 2026-08-30, so this run is recorded as an unresolved hosted-run flake/race investigation; CI is not claimed green.

- [CI run 33288273216](https://github.com/ojiepermana/myadmin/actions/runs/33288273216)
- [Contract run 33288273267](https://github.com/ojiepermana/myadmin/actions/runs/33288273267)
- [Security run 33288273229](https://github.com/ojiepermana/myadmin/actions/runs/33288273229)

- macOS signing/notarization was not available: no Developer ID certificate or
  notarization credentials were configured; the local artifact had no valid
  distribution signature or TeamIdentifier.
- Windows signing could not be run locally because `signtool.exe` is unavailable.
- The systemd unit is present but could not be operationally tested on macOS.
- `plutil -lint` passed for the launchd plist; an installed/running launchd
  service was not claimed.

These are environment or credential gates, not reasons to mark the affected
acceptance criteria as passed.

- `MANUAL-0055-AC7`: Codex reviewed `SECURITY.md`, the repository `README.md`, and `release/README.md` against the AC. They contain private vulnerability reporting guidance, checksum verification, platform artifact names, quick-start setup, data-directory guidance, operator/service documentation links, and unsigned-release caveats. This is a local content review, not a human publisher or hosted-release sign-off.
