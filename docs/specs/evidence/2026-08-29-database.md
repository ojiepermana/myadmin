# Database integration evidence — 2026-08-29

These runs used the disposable services from `tests/environments/` at commit
`4758eeeca3d4724a0f096aec69c4b918d3ea0a0c`. PostgreSQL 17.7 and 18.1, plus
MySQL 8.0.43 and 8.4.6, were healthy before the tests started.

PostgreSQL command:

```text
MYADMIN_POSTGRES_INTEGRATION=1 MYADMIN_POSTGRES_HOST=127.0.0.1 MYADMIN_POSTGRES_PASSWORD=<fixture-password> bun test tests/integration/postgresql tests/performance/postgresql-metadata.test.ts
```

PostgreSQL: **14 passed, 0 failed**, 51 assertions.

MySQL command:

```text
MYSQL_8_0_URL=mysql://fixture:<fixture-password>@127.0.0.1:3380/fixture?sslmode=require MYSQL_LATEST_URL=mysql://fixture:<fixture-password>@127.0.0.1:3384/fixture?sslmode=require bun test tests/integration/mysql
```

MySQL: **24 passed, 0 failed**, 144 assertions. Both MySQL fixtures used
`caching_sha2_password` over TLS; the explicit `sslmode=require` is intentional.

The run covers provider contracts, metadata, table designer, cancellation, and
database metadata performance. It does not prove every UI, visual, manual,
release, signing, or cross-engine end-to-end criterion.
