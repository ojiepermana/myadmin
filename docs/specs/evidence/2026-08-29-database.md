# Database integration evidence — 2026-08-29

These runs used the disposable services from `tests/environments/` at the
current checkout based on commit `79588db`. PostgreSQL 17.7 and 18.1, plus
MySQL 8.0.43 and 8.4.6, were healthy before the tests started.

Latest targeted rerun on 2026-08-30 used PostgreSQL 55433 and MySQL 3380/3384
with the disposable credentials and completed **94 pass, 2 skip, 0 fail, 739
assertions** across 21 integration/performance files. The two skips are the
MySQL principal cases requiring `CREATE USER`; all selected PostgreSQL,
MySQL metadata/performance, table/schema/database, export/import,
backup/restore, and table-operation tests passed. The PostgreSQL metadata
performance test also passed with its sub-second threshold.

PostgreSQL command:

```text
MYADMIN_POSTGRES_INTEGRATION=1 MYADMIN_POSTGRES_HOST=127.0.0.1 MYADMIN_POSTGRES_PASSWORD=<fixture-password> bun test tests/integration/postgresql tests/performance/postgresql-metadata.test.ts
```

PostgreSQL: **20 passed, 0 failed**, 79 assertions (baseline integration command).

MySQL command:

```text
MYSQL_8_0_URL=mysql://fixture:<fixture-password>@127.0.0.1:3380/fixture?sslmode=require MYSQL_LATEST_URL=mysql://fixture:<fixture-password>@127.0.0.1:3384/fixture?sslmode=require bun test tests/integration/mysql
```

MySQL: **36 passed, 0 failed**, 198 assertions. Both MySQL fixtures used
`caching_sha2_password` over TLS; the explicit `sslmode=require` is intentional.

The combined local suite was also run with both PostgreSQL and MySQL fixture
groups enabled, using the disposable root MySQL account for the opt-in
principal and privilege tests:

````text
621 passed, 0 failed, 4050 assertions

## Latest targeted administrative rerun

The focused MySQL provider and native backup suites were rerun against the
disposable root account because the ordinary `fixture` account intentionally
lacks `CREATE USER` and database-administration privileges. Both MySQL 8.0.43
and 8.4.6 passed the real principal lifecycle, grant/revoke effect, and native
backup roundtrip scenarios:

```text
MYSQL_8_0_URL=mysql://root:<fixture-root-password>@127.0.0.1:3380/fixture?sslmode=require
MYSQL_LATEST_URL=mysql://root:<fixture-root-password>@127.0.0.1:3384/fixture?sslmode=require
MYADMIN_MYSQL_SECURITY_INTEGRATION=1
````

Result: **24 passed, 0 failed, 78 assertions** across the two targeted files.
The initial non-root run's expected permission failures are retained as a
fixture-boundary observation; they are not counted as passing evidence.

````

The run covers provider contracts, metadata, table designer, cancellation, and
database metadata performance. Dedicated metadata performance now passes on PostgreSQL, MySQL 8.0, and MySQL latest, 3 tests and 21 assertions. It does not prove every UI, visual, manual,
release, signing, or cross-engine end-to-end criterion.

Additional Spec 0045 proof: the PostgreSQL provider integration test passed
the real role create/list/reset/alter/drop flow on PostgreSQL 18.1 (**12
passed** in the provider file). The MySQL provider integration test passed the
same account flow on MySQL 8.0.43 and 8.4.6 (**18 passed** in the provider
file). These MySQL security cases use the disposable root fixture account
because the normal `fixture` account intentionally lacks `CREATE USER`.
During this proof, the adapter was corrected to emit escaped SQL literals for
MySQL account password clauses because MySQL rejects parameter markers in
`CREATE USER` and `ALTER USER` password grammar.

Additional Spec 0046 proof: the opt-in PostgreSQL provider integration file
passed **14 tests**, including real table `GRANT SELECT` and `REVOKE SELECT`
effects. The corresponding MySQL provider integration file passed **22 tests**
across MySQL 8.0.43 and 8.4.6, including the same real grant/revoke effect
checks. The privilege case now also logs in as the created principal,
confirms `SELECT` succeeds and `INSERT` is rejected, then revokes the grant.
The PostgreSQL security adapter now uses the provider's `withTransaction()`
seam; issuing `BEGIN` as an ordinary Bun SQL query had returned a false
`failed` apply result on the real client.

Dengan URL root MySQL dan `MYADMIN_MYSQL_SECURITY_INTEGRATION=1`, lifecycle
principal yang sebelumnya dilewati juga lulus pada kedua versi: **22/22 test**,
**0 gagal**, **68 assertions**. Ini menutup fixture privilege untuk bukti lokal
Spec 0045/0046; authorization actor matrix dan E2E UI tetap terpisah.

Additional Spec 0037 proof: the same PostgreSQL opt-in file passed a real
table-page read with an exact count, covering metadata, rows, and `COUNT(*)`
after the count query was corrected to omit the primary-key ordering clause.
The real browser E2E also reads newly created PostgreSQL and MySQL tables
through the data-browser UI.

Additional Spec 0049/0050 proof: PostgreSQL native backup/restore passes
against the disposable PostgreSQL 18.1 fixture using the configured wrapper
tools. After granting the disposable MySQL `fixture` account the required
database privileges, MySQL 8.0/latest backup and database-management checks
also pass (**4 tests, 14 assertions**). This is local disposable-environment
evidence only.

## Native view CRUD

The PostgreSQL view unit test passed **3 tests**, and the provider integration
view test passed on PostgreSQL 18.1 and MySQL 8.0.43/8.4.6 (**3 tests**, **9
assertions**). The proof covers create, get definition, preview strategy,
replace, drop-create, apply, and drop. PostgreSQL drop-create now runs through
the provider `withTransaction()` seam; MySQL uses its native `ALTER VIEW`
path. This closes local provider proof for Spec 0044 AC-3, AC-5, and AC-8, but
does not prove the UI E2E, audit route, contract, security, or external review
requirements.

## Native MySQL backup dan restore

Rerun pada MySQL 8.0 port 3380 dan latest port 3384 lulus setelah privilege
database disposable disiapkan; native backup dan database-management checks
menghasilkan **4 pass, 14 assertions**. Hosted, external, dan clean-VM proof
tetap belum tersedia.

## Native security principal dan privilege

`bun test tests/integration/postgresql/provider.test.ts tests/integration/mysql/provider.test.ts` dengan PostgreSQL disposable ports 55433/55432 dan MySQL 8.0/latest ports 3380/3384, serta `MYADMIN_MYSQL_SECURITY_INTEGRATION=1`, pada rerun terbaru menghasilkan **37 pass, 0 skip, 0 fail, 116 assertions**. Bukti mencakup create/list/reset credential principal nyata serta grant/revoke dan inspeksi efek privilege nyata pada kedua engine. Test menggunakan cleanup untuk role/account dan object fixture. Ini menutup integration provider proof AC Spec 0045/0046 yang tercakup; E2E UI, authorization actor matrix, dan external security review tetap belum dianggap selesai.

## Native view CRUD

Unit PostgreSQL view test lulus **3/3**. Integration provider view test pada PostgreSQL 18.1 dan MySQL 8.0/latest lulus **3/3**, membuktikan create, get definition, preview strategy, replace/drop-create, apply, dan drop view pada engine nyata. PostgreSQL memakai change set `drop_create` di dalam `withTransaction`; MySQL memakai `ALTER VIEW`. Bukti ini menutup integration provider proof Spec 0044 AC-3, AC-5, dan AC-8 secara lokal, tetapi belum menggantikan E2E UI, audit route, contract/security matrix, atau external review.

## Current cross-engine integration rerun (recorded evidence)

- `PERF-0020-AC6`: synthetic 100.000-row audit query uses both required indexes and stays below the 3-second local threshold; `bun test tests/integration/audit/audit-admin.test.ts`.
- `PERF-0032-AC6`: PostgreSQL metadata page/search over a 2.000-table fixture stays below the 1-second local threshold; equivalent MySQL 8.0/latest metadata runs are recorded below.
- `PERF-0037-AC8`: PostgreSQL Data Browser page over a 1.000.000-row fixture returns 100 rows with `hasMore=true` in 38,99 ms, below the local 3.000 ms threshold; `MYADMIN_POSTGRES_INTEGRATION=1 bun test --isolate tests/performance/data-browser.test.ts`.
- `PERF-0047-AC3`: PostgreSQL ExportService streams a 100.000-row CSV job with heap delta `<256 MiB` and duration `<20 s`; `MYADMIN_POSTGRES_INTEGRATION=1 bun test --isolate tests/performance/export.test.ts`.
- `PERF-0047-AC8`: PostgreSQL CSV export contains exactly 100.000 data rows with correct file boundaries; `MYADMIN_POSTGRES_INTEGRATION=1 bun test --isolate tests/performance/export.test.ts`.
- `IT-0043-AC4`, `IT-0043-AC6`: actual `TableOperationsService` over PostgreSQL 55433 and MySQL 8.0/latest fixtures passes 3 tests and 24 assertions for real truncate/rename/drop, provider-specific impact, metadata invalidation, and the three success audit events.
- `IT-0042-AC3`, `IT-0042-AC4`, `IT-0042-AC5`, `IT-0042-AC8`: PostgreSQL and MySQL table-designer integration passes 6 tests and 37 assertions for real FK rules, composite unique constraints, metadata readback, and destructive constraint preview/apply.
- `IT-0050-AC4`, `SEC-0050-AC7`: RestoreService failure-path integration passes 3 tests and 12 assertions, including non-zero native restore exit, `restore.failed`/`restore.started` audit ordering, and absence of password material from audit; `bun test --isolate tests/integration/restore/restore.test.ts`.
- `IT-0049-AC4`: Native PostgreSQL/MySQL backup integration passes 3 tests and 15 assertions, including non-empty artefact and `CREATE TABLE` header validation before restore roundtrip.
- `SEC-0046-AC7`: PostgreSQL and MySQL security provider integration passes 37 tests and 132 assertions with TLS-enabled root fixtures, including principal login, SELECT success, INSERT denial, and revoke effect.
- `SEC-0049-AC4`: BackupExecutor unit proof passes 5 tests and 16 assertions, including synthetic password redaction from failed native-process errors and removal of partial artifacts.
- `IT-0035-AC8`: PostgreSQL provider integration lulus 14 test/47 assertions dan MySQL 8.0/latest lulus 22 test/82 assertions; kedua suite menjalankan `EXPLAIN SELECT 1` dengan plan non-empty lalu membatalkan long-running query pada engine nyata.
- `CT-0038-AC6`: Contract suite lulus 75 test dan 820 assertions; typed-row insert 422 response memuat `DATA_INVALID`, correlationId, dan detail kolom untuk conversion failure.
- `SEC-0038-AC6`: PostgreSQL/MySQL data query-builder tests lulus **12 test, 34 assertions**; invalid numeric input returns a column-specific error and binary input is rejected as read-only before SQL execution.
- `IT-0038-AC6`: PostgreSQL provider integration lulus **15 test, 49 assertions** dan MySQL 8.0/latest **24 test, 86 assertions**; typed insert succeeds against disposable databases while invalid numeric input is rejected before execution on all fixtures.
- `IT-0041-AC5`: PostgreSQL table-designer integration lulus **3 test, 14 assertions** dan MySQL 8.0/latest **6 test, 26 assertions**; incompatible boolean defaults are rejected with a field-level `incompatible_default` issue on real fixtures.
- `IT-0045-AC8`: PostgreSQL disposable integration verifies that a newly created login role can connect and execute `SELECT 1` with the rotated credential before the role is disabled and dropped; MySQL login proof remains open because the fixture rejects the principal connection.

Dengan PostgreSQL disposable 55433/55432 dan MySQL disposable 3380/3384 aktif, command berikut lulus **156 test, 0 gagal, 1.125 assertions** pada **32 file** dalam **35,58 detik**, tanpa skip:

```text
MYADMIN_POSTGRES_INTEGRATION=1 MYADMIN_POSTGRES_SECURITY_INTEGRATION=1 \
MYADMIN_MYSQL_SECURITY_INTEGRATION=1 \
MYADMIN_POSTGRES_CURRENT_PORT=55433 MYADMIN_POSTGRES_PREVIOUS_PORT=55432 \
MYSQL_8_0_URL=<mysql-8.0-fixture-url> MYSQL_LATEST_URL=<mysql-latest-fixture-url> \
bun test tests/integration tests/performance
````

Run ini mencakup object explorer, query execution/cancel/EXPLAIN, data browser read/write, schema and table designer, views, import/export/restore routes, provider metadata, security, native MySQL backup roundtrip, dan performance. Native PostgreSQL backup tools, hosted CI, visual/manual review, signing, notarization, dan clean-VM release tetap belum terbukti.

## Current full integration/performance rerun

Pada 2026-08-30, dengan PostgreSQL disposable 55433/55432, MySQL 8.0
3380, MySQL latest 3384, dan URL MySQL menggunakan TLS `require`, command
berikut lulus **182 pass, 0 fail, 1.322 assertions** across 38 files dalam
66,40 detik:

```text
MYADMIN_POSTGRES_INTEGRATION=1 MYADMIN_POSTGRES_SECURITY_INTEGRATION=1 \
MYADMIN_MYSQL_SECURITY_INTEGRATION=1 MYADMIN_POSTGRES_CURRENT_PORT=55433 \
MYADMIN_POSTGRES_PREVIOUS_PORT=55432 MYSQL_8_0_URL='mysql://root:<fixture>@127.0.0.1:3380/fixture?sslmode=require' \
MYSQL_LATEST_URL='mysql://root:<fixture>@127.0.0.1:3384/fixture?sslmode=require' \
bun test --isolate tests/integration tests/performance
```

Rerun ini mencakup privilege effect MySQL 8.0/latest, metadata, database dan
schema management, table designer, query cancellation/EXPLAIN, data browser,
import/export, backup/restore, security, dan performance terhadap fixture
disposable. Credential tetap direpresentasikan sebagai placeholder di evidence.

## Current root-credential integration/performance rerun

Rerun yang sama dengan URL root disposable MySQL (port 3380 dan 3384),
PostgreSQL disposable (port 55433), dan security integration opt-in lulus
**91 test, 0 gagal, 740 assertions** across 19 files. Run ini memberi bukti
real-engine tambahan untuk metadata/performance kedua versi MySQL, table
operations, export/import/restore routes, jobs, views, dan data-browser write
routes. Hasil ini tidak mengubah batasan bahwa browser visual/manual proof,
hosted CI, native PostgreSQL backup, signing/notarization, dan clean-VM release
belum tersedia.

Setelah perubahan route terakhir, focused server tests lulus **12/12** dengan
61 assertions, dan focused integration tests untuk object explorer, query
history, data browser, serta table operations lulus **10/10** dengan 110
assertions. `bun run typecheck` dan `bun run lint` juga lulus.

Capability audit juga menemukan dan memperbaiki descriptor stale pada kedua
provider: `importExport` kini dilaporkan `true` karena adapter Spec 0047/0048
sudah dikomposisikan oleh provider. Contract/provider regression lulus **44/44**
dengan 271 assertions; MySQL live-provider case tetap skip bila URL fixture
tidak disediakan.
Response capability dari kedua table-designer provider juga diselaraskan ke
`importExport: true`; regression table-designer/server lulus **15/15** dan
`bun run typecheck` lulus.

Rerun real-engine terarah setelah perubahan tersebut lulus **58/58** dengan
497 assertions pada PostgreSQL 18.1, MySQL 8.0.43, dan MySQL 8.4.6. Cakupan
meliputi capability, session/TLS, cancellation, principal, grant/revoke,
view, schema, table-designer, dan metadata/performance.

Database management integration baru menutup create/list/properties/drop
dengan nama fixture unik pada PostgreSQL 18.1 serta MySQL 8.0.43 dan 8.4.6:
**3/3 test**, **0 gagal**, 9 assertions. Setiap test membersihkan database
fixture yang dibuatnya sendiri.

Real table-operation integration juga lulus pada PostgreSQL 18.1, MySQL 8.0.43,
dan MySQL 8.4.6: **3/3 test**, **0 gagal**, **18 assertions**. Setiap test
membuat tabel unik, memasukkan dua row, menjalankan truncate, rename, dan drop
melalui `TableOperationsPort`, memeriksa impact metadata, lalu memverifikasi
tabel hasil rename benar-benar hilang. Coverage ini menutup bukti provider nyata
Spec 0043 AC-1, AC-2, dan AC-5; confirmation route serta browser UI tetap
diverifikasi terpisah.

Import/export adapter roundtrip baru juga lulus pada ketiga target engine:
source table diekspor melalui row stream, dimasukkan ke target dengan
parameterized `insertBatch`, lalu dibaca kembali dan dibandingkan. Hasilnya
**3/3 test**, **0 gagal**, 9 assertions, mencakup `IT-0047-AC2`,
`IT-0047-AC3`, `IT-0048-AC2`, `IT-0048-AC3`, dan `IT-0048-AC5`. Native
connection transaction seam juga membuktikan rollback pada ketiga engine.
ImportService kini memakai seam callback tersebut untuk SQL single transaction
dan CSV transaction, dengan fallback command untuk provider lama; unit/import
integration setelah refactor lulus **7/7**.

Service-level export/import roundtrip terbaru juga lulus pada ketiga target
engine melalui `tests/integration/export/real-export.test.ts`: CSV dan SQL
artifact dibuat oleh `ExportService`, lalu artifact SQL yang sama diproses oleh
`ImportService` setelah tabel dihapus. Hasilnya **3/3 test**, **0 gagal**, 42
assertions, dengan tiga statement dan dua row dipulihkan pada setiap engine.
Credential fixture tidak dicatat. Ini memperkuat `IT-0047-AC1`, `IT-0047-AC2`,
`IT-0047-AC5`, `IT-0047-AC8`, `IT-0048-AC2`, dan `IT-0048-AC8`.

## Real ImportService execution

ImportService kemudian diuji end-to-end terhadap PostgreSQL 18.1 dan MySQL
8.0.43/8.4.6 disposable fixtures. Setiap engine membuat tabel unik, mengunggah
CSV dan SQL, menjalankan job melalui JobManager, menunggu status `completed`,
dan membaca kembali dua row dari masing-masing tabel. Hasilnya **3/3 test**,
**0 gagal**, **18 assertions**. Cakupan langsung: `IT-0048-AC2`,
`IT-0048-AC3`, `IT-0048-AC5`, dan `IT-0048-AC8`.

```text
MYADMIN_POSTGRES_INTEGRATION=1 MYADMIN_POSTGRES_PORT=55433 \
MYSQL_8_0_URL=mysql://root:<fixture-root-password>@127.0.0.1:3380/fixture?sslmode=disable \
MYSQL_LATEST_URL=mysql://root:<fixture-root-password>@127.0.0.1:3384/fixture?sslmode=disable \
bun test tests/integration/import/import.test.ts
```

Run ini juga menemukan dan memperbaiki kehilangan receiver `this` ketika
ImportService memanggil method adapter nyata yang sebelumnya dilepas dari
provider object. Fake-provider unit test tetap lulus setelah perubahan.

## Native PostgreSQL backup/restore

Native backup/restore juga lulus terhadap fixture PostgreSQL 18.1 dengan
`pg_dump` dan `psql` versi 18.1 yang dijalankan melalui image Docker
`postgres:18`. Test membuat database sumber dan target unik, menjalankan dump
plain, restore streaming melalui subprocess native, lalu membandingkan row hasil
restore. Hasilnya **1 pass, 0 fail**, 4 assertions; database sementara dibersihkan
oleh test.

```text
MYADMIN_POSTGRES_INTEGRATION=1 MYADMIN_POSTGRES_CURRENT_PORT=55433 \
MYADMIN_POSTGRES_PREVIOUS_PORT=55432 \
bun test tests/integration/postgresql/backup-roundtrip.test.ts
```

Pada 2026-08-30, PostgreSQL metadata performance dengan fixture 2.000 tabel
lulus **1 test, 7 assertions**. Page 100 object dan search 50 object masing
masing memenuhi threshold <1 detik pada disposable PostgreSQL port 55433.

Rerun MySQL metadata pada 2026-08-30 terhadap fixture port 3380 (8.0) dan
3384 (latest) lulus **18 test, 362 assertions dalam 32,42 detik**. Ini mencakup
metadata object pages, server-side search pagination, dan performance catalog/
search pada kedua engine; hasil ini tetap local fixture evidence, bukan hosted
benchmark.

Pada 2026-08-30, native MySQL backup/restore roundtrip lulus **2 test, 0 fail,
8 assertions** untuk fixture 8.0 dan latest. Test membuat database sumber dan
target disposable, menjalankan `mysqldump`/`mysql`, lalu memverifikasi row hasil
restore. Credential command tetap dipasok lewat mekanisme provider dan tidak
dicatat dalam evidence.

Backup executor cancel regression pada 2026-08-30 lulus **6 test, 20
assertions**. Selain process double, test menjalankan subprocess `/bin/sh`
nyata melalui `Bun.spawn`; abort menghasilkan `AbortError` dan menghapus
partial artifact. Ini native-subprocess regression evidence, bukan clean-host
smoke atau cancel E2E UI.

Restore executor cancel regression pada 2026-08-30 lulus **3 test, 9
assertions**. Test tambahan menjalankan subprocess `/bin/sh` nyata melalui
`Bun.spawn`; abort membunuh proses dan melaporkan fase `cancelled` dengan
keadaan database mungkin parsial. Ini native-subprocess regression evidence,
bukan clean-host smoke atau cancel E2E UI.

ID integration native backup/restore yang dibuktikan pada 2026-08-30:

- `IT-0049-AC1`, `IT-0049-AC8`, `IT-0050-AC4`, `IT-0050-AC7`: PostgreSQL native roundtrip **1 pass, 4 assertions** dan MySQL 8.0/latest native roundtrip **2 pass, 8 assertions**.

- Rerun terarah 2026-08-30: `MYADMIN_POSTGRES_INTEGRATION=1 MYADMIN_POSTGRES_CURRENT_PORT=55433 MYSQL_8_0_URL='mysql://root:<fixture>@127.0.0.1:3380/fixture?ssl=disable' MYSQL_LATEST_URL='mysql://root:<fixture>@127.0.0.1:3384/fixture?ssl=disable' bun test --isolate tests/integration/postgresql/backup-roundtrip.test.ts tests/integration/mysql/backup-roundtrip.test.ts` lulus **3 pass, 0 fail, 15 assertions**; native backup, restore, dan row verification lulus pada PostgreSQL, MySQL 8.0, dan MySQL latest. Credential tetap diganti placeholder pada evidence.

- `IT-0049-AC1`: native PostgreSQL/MySQL roundtrip lulus pada fixture disposable.
- `IT-0049-AC3`: BackupService integration lulus **2 test, 12 assertions**; cancellation pada job berjalan mencapai state `cancelled`, menghapus partial artifact dari temporary store, mencatat audit cancelled, dan tidak membocorkan credential. Smoke cancel/native process pada environment bersih tetap belum terbukti.
- `CT-0049-AC1`, `CT-0049-AC2`, `CT-0049-AC7`: bundled OpenAPI contract suite lulus **2 test, 0 fail** untuk capability/tool status, backup queue request/response, unsupported response, dan artifact contract; native integration/smoke obligations tetap dipisahkan.
- `CT-0050-AC2`, `CT-0050-AC5`: restore contract suite lulus **2 test, 0 fail** untuk restore request schema, queued job response, serta owner-scoped validation/error response; native/UI/security obligations tetap dipisahkan.
- `SEC-0050-AC5`: RestoreService integration lulus **2 test, 8 assertions** untuk audit `restore.started`/`restore.completed` dan memastikan seluruh audit tidak memuat password fixture; full security matrix tetap dipisahkan.
- `IT-0049-AC8`: native PostgreSQL/MySQL roundtrip lulus pada fixture disposable.
- `IT-0050-AC4`: native PostgreSQL/MySQL restore subprocess lulus pada fixture disposable.
- `IT-0050-AC7`: native PostgreSQL/MySQL backup-restore roundtrip lulus pada fixture disposable.
- `UT-0041-AC8`: focused provider suite lulus **13 test, 0 fail, 55 assertions**; PostgreSQL dan MySQL mengompilasi seluruh katalog tipe kolom menjadi satu DDL create.
- `IT-0041-AC8`: disposable PostgreSQL lulus **4 test, 18 assertions** dan MySQL 8.0/latest lulus **8 test, 34 assertions**; setiap engine mem-preview DDL untuk seluruh katalog tipe kolom.
- `SEC-0041-AC8`: focused server table-designer suite lulus **18 test, 0 fail, 74 assertions**; audit perubahan berhasil tanpa credential atau SQL mentah.
- `IT-0038-AC9`: focused data-browser route suite lulus **8 test, 0 fail, 72 assertions**; typed insert/update/delete dan stale conflict diverifikasi untuk fixture provider PostgreSQL serta MySQL.
- `IT-0041-AC8`: focused table-designer integration lulus PostgreSQL **4 test, 18 assertions** dan MySQL 8.0/latest **8 test, 34 assertions**; disposable engine mem-preview DDL untuk seluruh katalog tipe kolom.

## Latest evidence ID alignment

Pada 2026-08-30, rerun dengan PostgreSQL disposable 55433 dan MySQL disposable
3380/3384 lulus **6 integration test, 0 gagal, 39 assertions** untuk table
designer dan operasi destructive. Cakupannya create/add/drop pada kedua engine
dan impact/truncate/rename/drop pada table nyata; test IDs yang dipetakan kini
mencakup `IT-0041-AC1`, `IT-0041-AC2`, dan `IT-0043-AC3`.

- `SEC-0042-AC6`: `bun test --isolate apps/server/test/table-designer.test.ts` lulus 5/5 (17 assertions); apply menolak drop constraint dan index tanpa konfirmasi, mengembalikan daftar destructive item, tidak memanggil provider apply, dan mencatat audit denied.
- `IT-0039-AC5`: PostgreSQL disposable run with the added invalid `NO_SUCH_ENCODING` assertion passed; MySQL rerun was not accepted because both configured ports rejected the fixture credential, so the MySQL half remains unproven in this run.
- `IT-0038-AC4`, `IT-0038-AC7`: `bun test --isolate tests/integration/data-browser/data-browser.test.ts` lulus 6/6 (52 assertions); write route memproses delete dengan identity, mencatat `data.rows_deleted` tanpa isi baris, dan mengembalikan `DATA_CONFLICT` pada stale update.
