# Bukti browser lokal 2026-08-29

## Query editor

Dedicated ResultGrid performance E2E (`PERF-0034-AC1`, `PERF-0034-AC8`) lulus
1/1: 5.000 typed rows dirender dalam **74,0 ms**, grid melaporkan 5.001 baris
termasuk header, dan row terakhir tidak dimaterialisasi di DOM. Ini membuktikan
virtualisasi pada browser lokal; bukan benchmark lintas browser, visual review,
atau accessibility sign off formal.

Contract settings proof (`CT-0052-AC1`, `CT-0052-AC3`) lulus **1 test, 5
assertions**, memvalidasi schema GET preferences, GET settings, dan response
204 untuk update preference.

Rerun terbaru `bun run test:e2e` pada working tree menjalankan **54 test** dan
menghasilkan **40 passed, 14 skipped, 0 failed** dalam 40,2 detik. Output run
mencakup proof browser untuk `E2E-0033`, `E2E-0034`, `E2E-0039`, `E2E-0043`,
`E2E-0047`, `E2E-0048`, `E2E-0051`, dan `E2E-0052`; empat belas skip adalah
skenario real-engine yang gated oleh `MYADMIN_REAL_DATABASE_E2E=1`.

`bun run test:e2e` menjalankan `E2E-0033-AC1` sampai `E2E-0033-AC9` pada `tests/e2e/web/zz-query-editor.spec.ts`. Skenario memakai response kontrak untuk PostgreSQL dan MySQL, termasuk autocomplete metadata, hasil bertipe, NULL, EXPLAIN, hasil truncated, dan queue export query. Ini membuktikan wiring browser dan SDK, bukan eksekusi provider nyata.

Rerun fokus `MYADMIN_REAL_DATABASE_E2E=1 bunx playwright test
tests/e2e/web/zz-real-query-editor.spec.ts tests/e2e/web/zz-real-security.spec.ts`
setelah penambahan flow database management, schema, view, operasi destructive
table, dan security principal menghasilkan **4 passed, 0 skipped, 0 failed**
dalam 2,2 menit. Proof mencakup properties/create/drop database serta
rename/truncate/drop table pada PostgreSQL dan MySQL, ditambah foreign-key,
composite-unique, index-drop, reset-password, dan exact-name principal drop.

Security principal create fixture dilakukan lewat API tervalidasi agar Chromium
autofill tidak mengubah dynamic password form; create-UI acceptance tetap tidak
diklaim.

Rerun real-browser security terbaru juga lulus **2/2 dalam 8,4 detik**.

Pada 2026-08-30, perbaikan interaksi ResultGrid yang mencegah click preview
menutupi dialog edit diuji melalui mock data-browser: **2 passed, 0 failed**
dalam 8,8 detik. Ini membuktikan regression pada edit/delete/conflict workflow
lokal; typed JSON/NULL terhadap database nyata masih belum terbukti.
Skenario `E2E-0045` membuktikan reset password dan drop principal PostgreSQL
melalui UI dengan exact-name confirmation; skenario `E2E-0046` membuktikan
grant dan revoke `SELECT` pada tabel nyata melalui UI serta introspeksi efeknya.
Credential dan password tidak dicatat dalam evidence. Create principal pada
real flow tetap seeded melalui API tervalidasi karena form create dinamis belum
stabil terhadap Chromium autofill.

Service-level principal audit test lulus **6 test, 15 assertions**, termasuk reset credential dan drop principal dengan action/target yang benar serta verifikasi bahwa credential sintetis tidak masuk ke audit event. Provider-engine audit matrix penuh belum diklaim.

Focused real-browser privilege coverage juga lulus **1 test dalam 6,6 detik**
untuk PostgreSQL disposable. Flow membuat tabel dan principal fixture,
membuka Security UI, memilih database/tabel, memilih `SELECT`, memeriksa
preview `GRANT SELECT`, menerapkan perubahan, membaca ulang grant efektif,
lalu meng-uncheck `SELECT`, memeriksa preview `REVOKE SELECT`, mengonfirmasi
revoke destructive, menerapkannya, dan memastikan grant tersebut hilang.
Ini membuktikan jalur UI grant/revoke nyata untuk `E2E-0046-AC2` dan
`E2E-0046-AC3`; actor login dan pembuktian SELECT/INSERT kini juga tersedia
di provider integration kedua engine, tetapi security sign-off penuh masih
terbuka.

`bun test apps/server/test/query-execution.test.ts tests/verification/query-realtime-acceptance.test.ts` lulus **18 test**. Bukti ini mencakup cancel execution aktif, preservasi statement selesai, idempotensi cancel, EXPLAIN melalui sesi tab, serta route acceptance Elysia. Real E2E API workflow juga membuktikan query tidur panjang dibatalkan pada kedua engine dan Explain memakai tab session yang sama; click-path langsung tombol Cancel/Explain dan capability-gate UI tetap belum dibuktikan secara khusus.

Dengan `MYADMIN_REAL_DATABASE_E2E=1`, `bunx playwright test tests/e2e/web/zz-real-query-editor.spec.ts --config=playwright.config.ts` lulus **2 test** dalam sekitar **2,7 menit**. Browser benar-benar login, membuat dan menghubungkan koneksi PostgreSQL disposable pada port 55433 serta MySQL disposable pada port 3380, membuat tabel fixture melalui query API, memilih masing-masing koneksi di query editor, menjalankan `SELECT 1`, membaca tabel tersebut melalui halaman data-browser, lalu melakukan insert, edit, dan delete baris melalui UI. Alur yang sama kemudian membuka Table Designer pada kedua engine, menambah kolom, rename kolom, meminta preview DDL, menghapus kolom dengan dialog konfirmasi destructive yang menyebut nama kolom, menerapkan setiap perubahan, dan memverifikasi struktur akhir di Data Browser. Skenario query juga membuktikan long query mencapai `running`, cancel menghasilkan `cancelled`, dan Explain memakai tab session yang sama pada kedua engine. Status `Complete`, result grid, exact total, heading tabel, nilai `browser fixture`, kolom `browser_renamed`, ketiadaan `browser_extra`, dan response mutation HTTP terverifikasi pada browser nyata. Test memakai data directory server disposable; cleanup HTTP sengaja tidak dijadikan bagian assertion karena endpoint lifecycle melalui dev proxy dapat menunggu, sementara server shutdown menutup provider handles. Perbaikan guard same-origin pada route query, data-browser, dan table-designer menyamakan perilaku dengan app guard agar request melalui Angular dev proxy tidak salah ditolak sebagai `CSRF_INVALID`.

Pada rerun real-engine terbaru, alur yang sama juga menjalankan `E2E-0043-AC1`, `E2E-0043-AC2`, dan `E2E-0043-AC5`: rename table dengan exact confirmation (`200`), truncate dengan exact confirmation (`204`), lalu drop table (`204`) pada PostgreSQL dan MySQL. Test ini lulus sebagai **1 pass** dalam **2,1 menit**. Ini membuktikan jalur API-backed browser pada kedua engine; dialog UI destructive khusus dan audit event yang tersimpan tetap membutuhkan proof terpisah.

Test yang sama juga membangun **2.000 table sintetis per engine** dengan prefix acak, menjalankan search dari halaman Object Explorer, memverifikasi 50 hasil awal dan 100 hasil setelah `Load more results`, mengukur search di bawah **5 detik**, lalu membuka hasil pertama sampai node table muncul pada lazy tree. Run terbaru lulus **1 test dalam 2,2 menit** dan menjadi proof browser nyata `E2E-0032-AC6` pada kedua engine. Durasi total terutama berasal dari pembuatan fixture disposable.

Focused rerun untuk `tests/e2e/web/settings-preferences.spec.ts`,
`z-shell-navigation.spec.ts`, `z-audit-admin.spec.ts`, dan
`zzz-object-explorer.spec.ts` lulus **8/8** dengan exit code 0. Ini memperbarui
bukti lokal untuk theme, shell resize/tab/menu/keyboard, audit admin, object
explorer, dan object search; visual approval formal serta manual accessibility
review tetap terbuka.

Pada run yang sama, hasil search dipilih secara eksplisit, node table di-expand, dan kolom `id` diverifikasi muncul dari describe metadata pada PostgreSQL dan MySQL. Ini memperluas proof ke `E2E-0031-AC8`; assertion memakai prefix fixture yang terurut agar tidak bergantung pada tabel sisa dari run disposable sebelumnya.

Run root suite terbaru dengan seluruh fixture database aktif menghasilkan **664 test, 0 skip, 0 gagal, 4.532 assertions** pada 143 file dalam 91,08 detik. Ini memperkuat matrix lokal setelah perubahan security capability UI; hasil ini tetap bukan bukti CI hosted, visual/manual acceptance, signing, atau publication.

Focused rerun `zz-query-editor.spec.ts` lulus **1/1 dalam 6,6 detik** dan secara eksplisit memverifikasi dua result-set melalui tab Statement 1/2, typed `NULL`, label `1 loaded of 2 rows`, badge truncation, ARIA row/column counts, navigasi keyboard antar-cell, row selection, clipboard TSV hasil `Copy selected rows`, immediate loaded-row export, full-export flow, Cancel, Explain sukses, error Explain HTTP 422 yang tampil di UI, dan capability gate Explain disabled dengan alasan (`E2E-0034-AC2` sampai `E2E-0034-AC7`, `E2E-0035-AC3`, `E2E-0035-AC5`, `E2E-0035-AC6`). Ini belum menjadi audit screen-reader/contrast atau performance formal.

## Security principal dan privilege

`bunx playwright test tests/e2e/web/zz-security.spec.ts --config=playwright.config.ts` lulus 1 test. Skenario membuktikan principal list, form dinamis, create, edit, reset password, drop dengan exact-name confirmation, serta payload request masing-masing operasi. Skenario privilege juga membuktikan catalog, change set, preview statement, apply result, payload `confirmRevoke`, serta tidak adanya opsi `WITH GRANT OPTION` atau column privilege V1. Ini membuktikan wiring UI/SDK dan V1 boundary, bukan efek grant atau revoke pada database nyata; efek engine nyata dibuktikan terpisah pada provider integration.

`bun run test:security` lulus **40 test, 968 assertions, 0 gagal**, mencakup header, rate limit, authorization matrix, at-rest storage, audit taxonomy, redaction, crypto, authentication, dan realtime checks. Ini adalah bukti lokal; hosted `security.yml`, clean environment, dan operational sign-off belum tersedia.

## Visual browser lokal

`bunx playwright test tests/e2e/web/login-session.spec.ts tests/e2e/web/z-connection-manager.spec.ts tests/e2e/web/zz-connection-lifecycle.spec.ts --config=playwright.config.ts` lulus **3 test** saat dijalankan mandiri. Login session, connection manager, dan lifecycle connection berhasil dirender serta dinavigasi tanpa bergantung pada urutan suite. Artifact screenshot dihasilkan pada `test-results/visual-0026-ac10.png` dan `test-results/visual-0027-ac7.png`. Ini adalah smoke visual lokal dengan state test, bukan review visual formal, baseline screenshot yang disetujui, atau audit accessibility penuh.

Test theme dan shell juga menghasilkan artifact visual lokal pada state terkunci: `test-results/visual-0014-light.png`, `test-results/visual-0014-dark.png`, dan `test-results/visual-0015-shell-1280.png`. Flow lulus **6 test** pada run gabungan, ditambah satu focused rerun theme; screenshot telah diinspeksi untuk memastikan halaman settings light/dark dan shell 1280px benar-benar ter-render. Artifact ini memperkuat bukti visual lokal Spec 0014/0015, tetapi bukan approval visual formal atau audit accessibility penuh.

Inspeksi ulang artifact tersebut pada working tree mengonfirmasi kontras light/dark, header/sidebar, tab shell, panel aktivitas, dan card layout terlihat utuh pada viewport yang direkam tanpa clipping yang terlihat. Ini adalah inspeksi visual lokal oleh agent, bukan sign-off reviewer atau pengujian screenshot baseline.

Pada rerun terbaru, ketiga artifact tersebut kembali dibuat dan diinspeksi pada
viewport 1280×720 (theme light/dark) dan 1280×900 (shell). Test menutup toast
success secara eksplisit sebelum capture; tidak terlihat overflow atau clipping
pada header, sidebar, tab host, card settings, maupun activity panel. Artifact
ini tetap bukan baseline visual yang disetujui atau audit accessibility penuh.

## Import CSV

`bunx playwright test tests/e2e/web/zz-import-export.spec.ts --config=playwright.config.ts` lulus 1 test. Dalam combined run export/import browser, tiga test lulus dalam **8,3 detik**, termasuk query/data export flow. Skenario import membuktikan upload file bounded, preview CSV server, mapping kolom, queue job melalui response kontrak, serta UI destructive import yang mengirim `truncateFirst: true` dan `confirmName` sama dengan nama table. Ini tidak membuktikan roundtrip database nyata atau efek truncate pada engine.

Integration provider nyata `tests/integration/export/real-export.test.ts`
berhasil dijalankan pada PostgreSQL disposable dengan **1 pass dan 14
assertions**, lalu pada MySQL 8.0 dan MySQL latest dengan **2 pass dan 28
assertions**. `ExportService` membuat job CSV dan SQL dari tabel nyata,
menunggu status `completed`, memeriksa row count, membaca artifact download,
dan memverifikasi header/data CSV serta DDL/INSERT SQL. Artifact SQL kemudian
diunggah ke `ImportService` setelah tabel di-drop; job import selesai dengan
tiga statement dan dua baris kembali terbaca. Ini memperkuat `IT-0047-AC1`,
`IT-0047-AC2`, `IT-0047-AC5`, `IT-0047-AC8`, `IT-0048-AC2`, dan `IT-0048-AC8`
pada tiga engine; belum menjadi proof E2E UI, scale-performance, atau security
khusus.

## Backup dan restore

`bunx playwright test tests/e2e/web/zz-backup-restore.spec.ts --config=playwright.config.ts` lulus **1 test dalam 7,0 detik**. Skenario membuktikan capability tooling, create backup, artifact listing, validasi restore, dan typed confirmation sebelum tombol restore aktif. Native roundtrip backup lulus terpisah pada PostgreSQL serta MySQL 8.0/latest; executor/artifact/provider suite juga lulus **5 test, 18 assertions**. Ini belum membuktikan smoke cancel/no-tool, route/security matrix penuh, atau acceptance external.

## Database management

`bunx playwright test tests/e2e/web/zz-database-management.spec.ts --config=playwright.config.ts` lulus 1 test. Skenario membuktikan properti database, opsi create dari provider, dan submit create melalui kontrak. Ini tidak membuktikan create atau drop pada engine database nyata.

Real-engine E2E pada `zz-real-query-editor.spec.ts` juga membuka halaman Database Management pada PostgreSQL dan MySQL disposable, membuat database melalui dialog UI, membuka properties database hasil create, lalu menghapusnya melalui exact-name confirmation. Flow `E2E-0039-AC1`, `E2E-0039-AC2`, dan `E2E-0039-AC5` lulus sebagai bagian dari test real workflow (**1 pass, 2,2 menit**). MySQL memakai root credential disposable karena user fixture tidak memiliki privilege CREATE DATABASE; credential tidak dicatat dalam evidence.

Flow real yang sama sekarang juga membuka Schema Management PostgreSQL, membuat schema, me-rename schema, lalu menghapusnya dengan exact-name confirmation. `E2E-0040-AC1`, `E2E-0040-AC3`, `E2E-0040-AC4`, dan `E2E-0040-AC6` lulus pada rerun **1 pass dalam 2,3 menit**. Perbaikan CSRF route mempertahankan sinyal `sec-fetch-site: same-origin` saat request melewati Angular development proxy; MySQL tidak dihitung sebagai schema-object proof karena adapter memodelkannya sebagai database.

## View editor

`bunx playwright test tests/e2e/web/zz-view-editor.spec.ts --config=playwright.config.ts` lulus 1 test. Skenario membuktikan pemilihan koneksi, validasi definisi, preview DDL, submit create view, drop preview, warning dependensi, dan exact-name gate melalui kontrak SDK. Ini tidak membuktikan DELETE view atau dependensi view pada engine nyata.

Focused real-browser E2E pada PostgreSQL disposable juga lulus **1 test dalam 6,6 detik** untuk create view melalui UI, validasi/preview DDL provider nyata, drop preview, exact-name confirmation gate, dan DELETE UI nyata dengan response `204`. Perbaikan stacking layer pada confirmation section membuat pointer click normal tidak lagi diintersep area CodeMirror. Ini membuktikan jalur PostgreSQL UI, bukan CRUD dua engine penuh atau sign-off visual/audit.

## Query history dan saved queries

`bunx playwright test tests/e2e/web/zz-query-history.spec.ts --config=playwright.config.ts` lulus 1 test. Skenario membuktikan membuka history ke tab query dengan SQL dan konteks, keterangan retention, konfirmasi dan penghapusan seluruh history, perpindahan ke saved queries, dialog pembuatan, dan submit saved query melalui kontrak SDK. Integration test `IT-0036-AC4 and AC6` juga membuktikan ownership isolation pada API dengan dua user sintetis. Ini belum membuktikan seluruh alur eksekusi browser atau matrix authorization pada database eksternal.

## Data browser

`bunx playwright test tests/e2e/web/zz-data-browser.spec.ts --config=playwright.config.ts` lulus 1 test. Skenario membuktikan bounded read, row identity editable, dialog insert, update berparameter dengan identity, delete berparameter setelah konfirmasi, dan queue export melalui kontrak SDK. Ini tidak membuktikan write roundtrip, transaction behavior, atau export skala besar pada engine nyata.

Coverage tersebut juga mengassert body request terbaru untuk pencarian teks `Ada`, filter kolom `name contains Ada`, sort `name asc`, dan payload export yang mempertahankan filter/sort. Ini membuktikan state read yang dikirim UI, tetapi belum membuktikan view read-only, visual/accessibility, atau alur browser dua engine penuh.

Test performance `PERF-0037-AC8` pada `tests/performance/data-browser.test.ts` lulus
**1 test, 0 gagal** terhadap PostgreSQL disposable dengan fixture **1.000.000 baris**.
Provider mengembalikan tepat 100 baris dengan `hasMore=true`, total 1.000.000, dan
pengukuran halaman pertama **38,99 ms** terhadap threshold `<3.000 ms`. Ini adalah
proof NFR-01 provider lokal; security matrix dan browser performance tetap terbuka.

## Object explorer error and retry

`bunx playwright test tests/e2e/web/zzz-object-explorer.spec.ts --config=playwright.config.ts`
lulus **1 test** setelah fixture dibuat mandiri dengan setup guard. Flow memverifikasi
ikon `table`, kegagalan metadata pada node `public` hanya menampilkan `HTTP request
failed` di node tersebut, lalu tombol `Try again` memuat ulang node dan menghapus error.
Artifact `test-results/visual-0031-explorer.png` dibuat pada viewport 1280×720 dan
diinspeksi; tree virtualized dan daftar table terlihat utuh tanpa clipping. Ini
memperkuat `E2E-0031-AC4` dan `VIS-0031-AC4`; performance virtualized serta
visual/accessibility formal tetap belum diklaim.

## Object search performance

Fixture performance nyata dengan **2.000 table** lulus pada PostgreSQL dan MySQL 8.0 serta MySQL 8.4. PostgreSQL menjalankan page listing dan pencarian paginated 50 hasil dengan ambang masing-masing **<1 detik**. MySQL menjalankan page listing dan pencarian paginated 50 hasil dengan ambang masing-masing **<5 detik**. Command PostgreSQL `MYADMIN_POSTGRES_INTEGRATION=1 ... bun test tests/performance/postgresql-metadata.test.ts` lulus **1 test**, dan command MySQL dengan URL root untuk kedua fixture lulus **18 test**. Proof browser 2.000 table pada kedua engine tercatat pada E2E nyata di bagian Query editor.

## Table destructive operations

Test ketiga dan kelima pada `tests/e2e/web/zzzz-table-designer.spec.ts` lulus untuk Spec 0043. Skenario membuktikan impact metadata, estimasi row, exact-name confirmation, payload truncate, dependency warning, dan payload rename melalui kontrak SDK. Ini tidak membuktikan perubahan atau penghapusan data pada engine nyata.

## Table indexes and constraints

Test keempat pada `tests/e2e/web/zzzz-table-designer.spec.ts` lulus untuk Spec 0042. Skenario membuktikan pembuatan composite index berurutan dan payload `addIndex` pada preview DDL. Bukti E2E dua engine dicatat pada flow real di bawah.

E2E nyata Spec 0042 juga lulus pada PostgreSQL 18.1 dan MySQL 8.0.43 dalam **1 test**, **3,0 detik**. Flow membuat fixture parent/child dan index, lalu melalui table designer memilih target FK dari object search, menetapkan ON DELETE CASCADE, mempreview dan menerapkan FK, membuat composite unique, serta menghapus index melalui destructive confirmation. Ini membuktikan browser proof `E2E-0042-AC3`, `E2E-0042-AC5`, dan `E2E-0042-AC8` pada kedua engine; contract/security penuh, semua failure boundary, visual/manual, dan external proof tetap belum tertutup.

Pada 2026-08-30, `bun run test:e2e -- tests/e2e/web/zzzz-table-designer.spec.ts` lulus **6 test, 0 gagal dalam 8,0 detik**. Skenario `E2E-0042-AC2` memuat index lama, mengubah nama dan kolom komposit, lalu memverifikasi payload `dropIndex` + `addIndex` serta dua statement SQL pada preview. Ini membuktikan transparansi perubahan index pada mock browser; bukan pengganti E2E real-engine atau reviewer acceptance.

Suite tersebut kemudian lulus **8 test, 0 gagal dalam 8,7 detik** setelah penambahan `E2E-0041-AC4`: penghapusan `legacy_code` memunculkan dialog destructive, menampilkan kolom terdampak, menahan apply sampai konfirmasi, lalu mengirim `confirmDestructive: true` dengan change set `drop`. Ini membuktikan browser proof mock untuk destructive column apply.

Rerun suite yang sama pada 2026-08-30 lulus **9 test, 0 gagal dalam 9,4 detik**. Skenario `E2E-0043-AC3` menampilkan view dan incoming-FK dependency pada dialog drop, menjaga CASCADE tetap tidak ditawarkan, menolak konfirmasi kosong, lalu mengirim `confirmName` yang tepat dan berpindah ke Explorer setelah sukses. Ini membuktikan UI dependency/confirmation pada mock browser; rejection oleh engine nyata dan audit/security tetap dibuktikan terpisah.

Skenario tambahan `E2E-0043-AC6` pada `tests/e2e/web/zzzz-table-designer.spec.ts` lulus **1 test dalam 6,3 detik**. Dengan tab data `accounts` terbuka, operasi truncate berhasil dan tab tersebut menampilkan marker `Reload required`. Ini menutup proof lokal untuk stale-tab invalidation; proof visual/accessibility dan security tetap terpisah.

## Schema management

`bunx playwright test tests/e2e/web/zz-schema-management.spec.ts --config=playwright.config.ts` lulus 1 test. Skenario membuktikan schema capability gating, provider schema listing, dialog create, rename dengan warning dampak, drop dengan exact-name confirmation, serta payload create/rename/drop melalui kontrak SDK. Ini tidak membuktikan operasi schema pada PostgreSQL nyata.
Perbaikan pendukung: mendaftarkan `SchemasClient` yang sebelumnya diekspor SDK tetapi belum tersedia dari `provideMyadminSdk()`, yang menyebabkan `NG0201` saat lazy feature dibuat.

Regresi `bun run test:e2e` lulus dengan **39 test passed dan 14 skipped**; empat belas test skipped adalah browser-real yang memang membutuhkan `MYADMIN_REAL_DATABASE_E2E=1`. Test mock preferences dan workspace menjaga session shell agar tidak dipengaruhi state test sebelumnya.

`VIS-0015-AC8` visual shell boundary proof pada 2026-08-30 lulus **1/1**: viewport 1024px
tetap memakai layout desktop tanpa clipping, sedangkan viewport 1023px membuka
sidebar sebagai overlay dengan backdrop, lalu menutupnya kembali melalui aksi
close. Screenshot tersimpan sebagai `test-results/visual-0015-shell-1024.png`.

- `VIS-0015-AC8`: visual shell boundary lulus pada viewport 1024px dan 1023px; screenshot tersimpan di `test-results/visual-0015-shell-1024.png`.

## Regression lokal 29 Agustus 2026

Full root regression dengan fixture PostgreSQL dan MySQL lulus **662 test, 0 fail, 4.526 assertions** dalam 90,56 detik. `bun run test:security` lulus **40 test**, dan `bun run test:contract` lulus **71 test**. Quality gates `bun run lint`, `bun run format:check`, `bun run typecheck`, `bun run check:boundaries`, `bun run check:manifests`, dan `git diff --check` juga lulus. Bukti ini tetap merupakan validasi lokal; tidak mengubah kebutuhan CI hosted, performance formal, visual/accessibility, atau release proof.

## Packaging dan distribution lokal

`bun run build:web` lulus dan menghasilkan `dist/web/`. `bun run build:binaries` sebelumnya lulus untuk lima target: linux-x64, linux-arm64, macos-x64, macos-arm64, dan windows-x64. Rerun release build mengompilasi ulang macOS ARM64, `bun run checksums` menghasilkan lima SHA-256 checksum, `bun run release:sizes` menghasilkan laporan ukuran, dan packaging/release suite lulus **18 test, 86 assertions**. Smoke binary macOS ARM64 lulus health, embedded SPA, setup, login, auth, shutdown, dan doctor; setelah fixture tersedia, smoke dengan URL PostgreSQL disposable juga lulus koneksi database.

`bun run smoke:binary -- --binary dist/binaries/macos-arm64/myadmin --database-url <postgres-fixture-url>` juga lulus koneksi ke database disposable. Fixture PostgreSQL lulus 19 test dan MySQL 8.0/8.4 lulus 36 test dengan TLS `sslmode=require`, termasuk cancellation provider dan metadata performance. CI hosted, Docker registry, signing/notarization, dan clean-VM release proof tetap belum terbukti.

Rerun binary ARM64 terbaru: smoke tanpa database dan smoke `--require-database` sama-sama lulus health, embedded SPA, setup/login/auth, shutdown, dan doctor; run kedua juga memverifikasi koneksi PostgreSQL disposable. Ini tetap bukan bukti clean VM, signing, atau hosted release.

## Batas evidence

Bukti browser di atas adalah contract mock. Acceptance untuk engine nyata, visual review, accessibility penuh, performance, CI hosted, dan external release tetap belum ditandai done.

## Focused shell, theme, audit, and explorer E2E

Pada 2026-08-29, command berikut dijalankan terhadap local web server:

```text
bunx playwright test tests/e2e/web/settings-preferences.spec.ts tests/e2e/web/z-shell-navigation.spec.ts tests/e2e/web/z-audit-admin.spec.ts tests/e2e/web/zzz-object-explorer.spec.ts --config=playwright.config.ts
```

Hasil: 8 test passed, 0 failed dalam 13.0 detik. Proof mencakup theme light/dark/system dan sinkronisasi preference, resize/fold panel, tab isolation, context menu dan keyboard flow, audit event hasil login gagal, serta lazy tree, pagination, dan provider-driven object search. Settings dan audit test juga menginisialisasi disposable admin bila server masih berada pada setup state agar setiap test independen. Ini tetap evidence E2E lokal; visual review formal, accessibility penuh, performance khusus, dan manual/external proof tidak dianggap tertutup.

Rerun terarah seluruh mock feature suites pada working tree menghasilkan
**21 passed, 0 failed** dalam 21,3 detik. Cakupan mencakup theme, shell, audit
admin, backup/restore safeguards, data browser, import, query editor/result
grid/cancel/explain, history, schema, security, view, object explorer/search,
dan table designer/destructive flows. Ini memperkuat local mock E2E evidence;
real-engine, visual baseline, accessibility penuh, performance, dan
hosted/manual acceptance tetap terpisah.

Rerun terbaru real-engine browser menjalankan **8 test** pada disposable
PostgreSQL dan MySQL dan selesai dengan status Playwright `passed`, tanpa test
gagal. Cakupan mencakup query/cancel/EXPLAIN, object search, data browser,
database/schema/table operations, view, index/constraint, principal reset/drop,
serta grant/revoke privilege PostgreSQL dan MySQL. Actor authorization
SELECT-versus-INSERT juga dibuktikan oleh provider integration kedua engine.
Ini adalah bukti browser lokal nyata; visual/accessibility penuh, performance
baseline, dan hosted acceptance tetap belum diklaim.

Rerun `MYADMIN_REAL_DATABASE_E2E=1 bun run test:e2e -- tests/e2e/web/zz-real-security.spec.ts`
menyelesaikan **4 test, 0 gagal**. PostgreSQL dan MySQL principal reset/drop
serta grant/revoke table flow lulus melalui UI nyata; hasil ini menutup bukti
real-browser untuk E2E-0045 dan E2E-0046 yang dicakup test tersebut. Security
sign-off, actor matrix tambahan, dan hosted proof tetap terpisah.

Real authenticated export/import route roundtrip juga lulus **2 test, 0 gagal**
untuk PostgreSQL dan MySQL. Alurnya membuat tabel, mengekspor SQL, mengunduh
artifact, menghapus tabel, mengunggah artifact yang sama, lalu menjalankan job
import sampai selesai. Verifikasi query akhir memeriksa kembali dua row typed
(`Ada` dan `Grace`); visual import/export dan performance skala besar tetap
terpisah.

Authenticated real restore route roundtrip PostgreSQL dan MySQL lulus **2 test,
0 gagal** dalam 6,6 detik. Alurnya mengunggah SQL dump, memvalidasi engine,
menjalankan restore job ke database disposable yang sudah ada, lalu memverifikasi
row hasil melalui query execution dan membersihkan tabel/koneksi. Ini membuktikan
route/job restore kedua engine lokal; backup-to-restore UI penuh, visual,
security matrix, dan smoke tetap terpisah.

Mock UI backup/restore safeguard juga lulus **1 test, 0 gagal** dalam 6,3 detik
pada viewport 1280x1200. Test kini memverifikasi submit restore dengan artifact,
target database, typed confirmation, dan job restore yang tampil; ini tetap
local mock UI evidence dan tidak menggantikan real backup-to-restore flow.

Native MySQL backup-to-restore gzip juga lulus **1 test, 0 gagal** dalam 6,6 detik.
Test membuat database sumber disposable, membuat artifact dengan `mysqldump`,
mengunduh dan memeriksa header gzip-nya, me-restore ke database baru memakai
`mysql`, memverifikasi row, lalu membersihkan kedua database. PostgreSQL native
backup-to-restore belum dapat dijalankan karena `pg_dump` tidak tersedia pada
host ini.

Rerun terbaru yang ditargetkan ke `zz-real-query-editor.spec.ts` dengan
`MYADMIN_REAL_DATABASE_E2E=1` lulus **4 test dalam 2,4 menit**. Ini mencakup
mutation data-browser dua engine (`E2E-0038-AC9`), database/schema operations,
view create/drop, serta FK/composite-unique/index operations pada PostgreSQL
disposable port 55433 dan MySQL disposable port 3380. Cleanup dilakukan oleh
global teardown; tidak ada credential yang dicatat.

Pada 2026-08-30, `bunx playwright test tests/e2e/web/zz-data-browser.spec.ts`
lulus **2 test dalam 7,8 detik**. Mock browser ini menambahkan failure-path
conflict: update kedua menerima HTTP 409 `DATA_CONFLICT`, pesan untuk reload
tampil, dialog edit dapat ditutup, lalu alur delete dan export tetap berjalan.
Ini memperkuat proof browser conflict untuk E2E-0038-AC3; typed-editor penuh,
conflict real-engine, dan security/audit proof tetap terpisah.

Test yang sama juga memverifikasi column picker: menghapus lalu mengaktifkan
kembali kolom `id` menghasilkan payload `columns` yang sesuai, dan export
membawa urutan kolom dari state UI (`name`, `id`). Full E2E kemudian juga
mengonfirmasi pagination ke Page 2 pada harness layout penuh; request paging
tercatat dengan `limit: 100` dan `offset: 100`.

Pada 2026-08-30, rerun gabungan browser mock untuk shell, audit, backup/restore,
data browser, query editor/history, database/schema management, security,
view editor, object explorer/search, dan table designer lulus **20 test dalam
21,2 detik** tanpa failure. Ini adalah evidence lokal terhadap alur UI yang
terdaftar; visual review formal, accessibility penuh, performance formal,
real-engine coverage, dan manual/external acceptance tetap tidak tertutup.

Contract export rerun pada 2026-08-30 lulus **1 test, 9 assertions**. Test
memanggil endpoint `/export/:id/download` melalui app nyata dan memverifikasi
status, `text/csv`, `Content-Disposition` dengan nama artifact bermakna, serta
isi file; ini memperkuat `CT-0047-AC5`, sementara ownership/expiry/security
matrix penuh tetap terbuka.

Rerun ExportService pada 2026-08-30 juga menguji ownership download: actor
berbeda menerima `Export was not found.` untuk artifact milik actor lain.
Assertion ini berjalan bersama audit completion dan redaction checks sebagai
`SEC-0047-AC5`/`SEC-0047-AC7`; expiry dan cleanup policy tetap belum penuh.

Export service unit/security rerun pada 2026-08-30 lulus **3 test, 11
assertions**. Completion audit `export.completed` diverifikasi dengan actor,
target type, format, row count, dan source terstruktur; isi row serta password
tidak muncul dalam audit event. Ini memperkuat `SEC-0047-AC7`; audit matrix
integration dan ownership/expiry tetap terbuka.

Rerun expiry ExportService pada 2026-08-30 memakai clock terkontrol: artifact
melewati retention, menghasilkan error expired, dan file artifact terhapus dari
temp directory. Ini memperkuat `SEC-0047-AC5`; cleanup artifact target tetap
berjalan setelah error dikembalikan.

Perbaikan regresi expiry pada 2026-08-30 membuat `download()` mengecualikan
artifact target dari cleanup awal, sehingga artifact kadaluarsa menghasilkan
error expired yang benar sebelum file dihapus. Rerun ExportService lulus **4
test, 15 assertions** dan HTTP export integration lulus **1 test, 8
assertions**.

Packaging rerun 2026-08-30 lulus **7 test, 19 assertions**. Test baru
memverifikasi size report untuk seluruh lima target binary, artifact path yang
benar, sorting deterministik, serta penulisan `1 byte` versus `N bytes`;
perbaikan formatter diterapkan di `scripts/build/report-sizes.ts`.

ResultGrid performance rerun pada 2026-08-30 lulus **1 test dalam 5,9 detik**;
dataset 5.000 typed rows ter-render dalam **70,4 ms**, `aria-rowcount` tetap
5.001, dan row terakhir tidak dimuat ke DOM berkat virtualisasi. Ini tetap
local performance evidence dengan fixture mock, bukan benchmark lintas host
atau visual/accessibility sign-off.

Full browser suite pada 2026-08-30 lulus **34 test, 0 gagal** dalam 36,1
detik dengan **14 skip**. Skip adalah skenario real-engine yang membutuhkan
environment fixture eksplisit; run ini menjadi regression evidence untuk shell,
settings, query, data browser, security, backup/restore, object explorer, dan
table designer, bukan bukti visual/manual atau hosted acceptance.

Pada 2026-08-30, real-engine browser batch untuk security dan import/export
lulus **6 test dalam 11,8 detik**. PostgreSQL dan MySQL masing-masing
memverifikasi principal reset/drop, grant/revoke privilege tabel, serta
roundtrip export/import SQL melalui UI dan route aplikasi terhadap fixture
database nyata. Ini memperkuat E2E-0045-AC1/2/4/5, E2E-0046-AC2/3, dan
E2E-0047/0048-AC8; proof hosted, visual, performance skala besar, dan
environment eksternal tetap tidak tertutup.

Backup/restore mock E2E pada 2026-08-30 lulus **1 test dalam 6,6 detik**.
Selain create/no-tool/restore safeguards, test memverifikasi `POST
/api/v1/jobs/backup-job-1/cancel`, state `Cancelled`, dan hilangnya tombol
cancel. Ini adalah browser mock proof; native cancel cleanup dan operational
smoke tetap terpisah.

Rerun real-engine typed data-browser terpisah pada 2026-08-30 lulus **1/1
dalam 7,3 detik**. PostgreSQL dan MySQL masing-masing membuka table nyata,
mengedit JSON melalui dialog bertipe, lalu menyimpan NULL eksplisit pada kolom
nullable. Skenario yang sama menghapus row melalui request kedua lalu membuktikan
stale PATCH menerima HTTP 409 dan UI menampilkan instruksi reload. Ini menutup
bukti lokal untuk typed JSON/NULL dan conflict real-engine; security matrix,
visual review, dan acceptance hosted tetap terbuka.

Real PostgreSQL view E2E pada 2026-08-30 lulus **1/1 dalam 6,6 detik** dengan
assertion audit tambahan: `view.created` dan `view.dropped` ditemukan setelah
lifecycle UI. Ini memperkuat E2E-0044-AC5 secara lokal; security matrix dan
formal acceptance tetap terpisah.

Real table destructive lifecycle E2E pada 2026-08-30 lulus **1/1 dalam 2,2
menit**. Setelah rename, truncate, dan drop pada PostgreSQL serta MySQL, test
membaca audit endpoint dan memverifikasi event `table.renamed`,
`table.truncated`, serta `table.dropped`. Stale-tab, security matrix, dan
formal visual/accessibility review masih terpisah.
